# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

Greenfield. There is **no source code yet** — only the `.claude/` scaffold. The product to build is a multi-tenant taxi fleet system (dispatcher web app, driver PWA, customer PWA, one .NET backend, one Postgres DB) specified in `.claude/state/00-PROJECT-CONTEXT.md` and the numbered assignment files `.claude/state/01-*.md` … `08-*.md`. `.claude/state/README.md` gives the assignment dependency order (01 → 08∥02 → 03 → 04 → 06 → 05 → 07).

## How context loads (important asymmetry)

- `.claude/rules/*.md` are **auto-injected into every session** as project instructions.
- `.claude/state/00-PROJECT-CONTEXT.md` and the numbered assignments are **NOT auto-loaded** — read them deliberately before any feature work. `00-PROJECT-CONTEXT.md` declares itself the source of truth over the assignments.

## Stack decision (resolved 2026-09-10 — see docs/decisions.md)

The user decided: **.NET 10 + FastEndpoints**. The `.claude/rules/` conventions win over the original spec text; `00-PROJECT-CONTEXT.md` has been updated to match. Concretely:

- FastEndpoints REPR endpoints (`rules/api-design.md`), FastEndpoints `Validator<TRequest>` validators (`rules/validation.md`).
- .NET 10 / C# 14, EF Core 10 + Npgsql.
- No `Domain/` folder — vertical slices under `Features/`, shared logic (e.g. `OrderStateMachine`) in `Common/`, DbContext/entities/migrations in `Infrastructure/` (`rules/architecture.md`).
- Illegal order transitions are expected errors: failure result → `AddError(...)` + `Send.ErrorsAsync(409, ct)` — no `InvalidTransitionException`, no control-flow exceptions (`rules/error-handling.md`).
- Frontend stays React (per spec §4); there is no Flutter pipeline.

Note: `rules/logging.md`'s canonical property-name table (`InviteId`, `MeetupId`, …) comes from a different app; the style rules apply but use taxi-domain names (`OrderId`, `DriverId`, `FleetId`, …).

## Commands

No build exists yet. Once `/api` is scaffolded, the quality gate (from the conductor pipeline Phase 5 and the Definition of Done in `00-PROJECT-CONTEXT.md` §12) is:

```bash
dotnet build -warnaserror              # blocking
dotnet test                            # blocking — integration tests use Testcontainers, requires Docker running
dotnet format --verify-no-changes      # informational
```

Frontend (`/web`, React + Vite, once it exists): `eslint --max-warnings 0`, Vitest for logic, Playwright for the three critical flows.

## The /conductor pipeline

`/conductor <feature>` (`.claude/skills/conductor/SKILL.md`) orchestrates UC → design → implement → review with user-approval gates:

- State lives in `.claude/state/pipeline.json`; specs in `docs/specs/{todo,in-progress,done}/`.
- Agents: `designer` → `design-reviewer` → `backend-developer` / `frontend-developer` (routed by each WI's `lane: "api" | "web"`; TDD, stage changes, **never commit**) → `impl-reviewer`. Each writes `.claude/state/handoff-<agent>.json`; only the conductor writes `pipeline.json`. (Historical `handoff-developer-*.json` files predate the 2026-09-12 rename.)
- Only the conductor gates commit/PR; nothing commits without explicit user confirmation.

The `gc-*` skills (`gc-feature`, `gc-tdd`, `gc-debug`, `gc-migrate`, `gc-review`) are standalone .NET workflows usable outside the pipeline.

## Cross-cutting invariants (from the spec — apply to all backend work)

1. **Multi-tenancy**: every tenant-owned table has `FleetId`; EF Core global query filters enforce isolation; every feature ships a tenant-isolation integration test (fleet A cannot read fleet B). See `00-PROJECT-CONTEXT.md` §7.
2. **Order state machine**: all order status changes go through one class (`OrderStateMachine`) — never set `order.Status` directly. Every transition writes an `order_events` row and broadcasts `OrderChanged` on SignalR, in one transaction. The `taxi-order-state-machine` skill is the source of truth for statuses, transitions, actors, and side effects — read it whenever code touches order status.
3. **Conventions** (spec §6): timestamps `timestamptz` UTC; money as integer CZK; phone numbers E.164; tables/columns `snake_case`, JSON `camelCase`; Czech-first UI.

## Project-specific facts (discovered during implementation — do not rediscover)

- **FakeTimeProvider namespace**: the NuGet package is `Microsoft.Extensions.TimeProvider.Testing` but the actual C# namespace is `Microsoft.Extensions.Time.Testing`. Use `using Microsoft.Extensions.Time.Testing;` — the package-name spelling CS0234s.
- **Testcontainers 4.x PostgreSqlBuilder**: the parameterless `new PostgreSqlBuilder()` constructor is `[Obsolete]` and causes a CS0618 warning-as-error under `-warnaserror`. Always pass the image: `new PostgreSqlBuilder("postgres:16-alpine")`.
- **Route entity naming conflict**: `Taxi.Api.Infrastructure.Entities.Route` conflicts with `Microsoft.AspNetCore.Routing.Route` in files that have both using namespaces. Use a `using RouteEntity = Taxi.Api.Infrastructure.Entities.Route;` alias in files that need both.
- **EF Core package version conflict**: `EFCore.NamingConventions 10.0.1` and `Npgsql.EntityFrameworkCore.PostgreSQL 10.0.3` bring in EF 10.0.1/10.0.4 respectively, conflicting with `Microsoft.EntityFrameworkCore.Design *` (10.0.12). Fix: add `api/Directory.Build.props` that pins all EF Core packages to 10.0.12 via `PackageReference Update`, AND add explicit `Microsoft.EntityFrameworkCore` + `Microsoft.EntityFrameworkCore.Relational` at 10.0.12 directly to the test project.
- **FluentAssertions + expression trees**: `.Where(e => e.InnerException is PostgresException pe && pe.SqlState == "23505")` does not compile in expression tree context (CS8122). Instead use `.ThrowAsync<T>()` then `.Which.InnerException.Should().BeOfType<PostgresException>().Which.SqlState.Should().Be("23505")`.
- **dotnet ef migrations path**: use `--output-dir Infrastructure/Migrations` when adding migrations from inside `api/src/Taxi.Api`; omitting it puts the migration at the project root.
- **UUIDv7 value generator**: configured in `TaxiDbContext.OnModelCreating` by iterating `Model.GetEntityTypes()` and setting the generator on any property named "Id" of type `Guid`. FleetSettings.FleetId is excluded because its property name is not "Id".
- **Concurrency token choice (WI-03)**: used an explicit `int Version` column with `.IsConcurrencyToken()` (not xmin). `OrderService` (WI-07) is responsible for incrementing `Version` before saving.
- **JsonDocument for jsonb (WI-03)**: `OrderEvent.Payload`, `Zone.Polygon`, and `AuditLog.Diff` use `System.Text.Json.JsonDocument?` mapped with `.HasColumnType("jsonb")`. Callers must dispose `JsonDocument` after use or use `JsonDocument.Parse` for transient values.
- **UserRole.System (WI-07)**: A `System` member was added to `UserRole` enum for the Timeout transition (no human actor). EF stores enums as strings; adding a new member requires no migration (new string value is backward-compatible). `Actor.System` is a static property on `Actor`.
- **OrderEvent.ActorUserId FK (WI-07)**: `order_events.actor_user_id` has a FK to `users`. Integration tests seeding OrderEvents must ensure the actor's UserId is a real user row. Use a seeded dispatcher user's ID in the Actor, not a random Guid.
- **Version increment in OrderService (WI-07)**: `order.Version++` is called in `OrderService.TransitionAsync` immediately before `SaveChangesAsync`. EF adds Version to the UPDATE WHERE clause for optimistic concurrency but does NOT auto-increment it. Always increment explicitly.
- **PriceOverridden event (WI-07)**: When a Fixed-price order is completed with a different price (and a valid override reason), TWO OrderEvent rows are saved: one `Completed` + one `PriceOverridden`. `TransitionResult.Events` carries both. This supersedes the SKILL.md 'exactly one row' checklist.
- **Concurrency conflict test pattern (WI-07)**: To trigger `DbUpdateConcurrencyException`, pre-load the entity into scope2's DbContext BEFORE scope1 commits. Then scope1 saves (bumps Version), and scope2's subsequent `SaveChangesAsync` detects the stale Version token and throws. Sequential calls without pre-loading won't conflict because scope2 re-loads the updated version.
- **ICurrentTenant / query filters (WI-04)**: `TaxiDbContext` constructor takes `ICurrentTenant` — the design-time factory passes a `NullCurrentTenant` stub (FleetId=null). All 10 non-nullable tenant entities implement `ITenantEntity`; User/PushSubscription do NOT (nullable FleetId). Seeding and raw DbContext scopes (null-tenant) bypass SaveChanges guard. Use `IgnoreQueryFilters()` for cross-tenant reads in jobs/seed. Tests access `CurrentTenant` concrete via `InternalsVisibleTo`; `GetRequiredService<CurrentTenant>()` sets FleetId before using the scope's DbContext.
- **Middleware execution order (WI-04)**: `TenantResolutionMiddleware` is registered with `app.UseMiddleware<TenantResolutionMiddleware>()` _after_ `app.UseAuthentication()` (needs JWT claims) and _before_ `app.UseAuthorization()` (consistent with FastEndpoints ordering). Scoped `CurrentTenant` is written by middleware and read by `TaxiDbContext`; both must be in the same DI scope.
- **FastEndpoints enum serialization (WI-09)**: FastEndpoints uses System.Text.Json without `JsonStringEnumConverter` by default. Any endpoint request that accepts an enum property (e.g. `PaymentType?`) and receives a JSON string value (e.g. `"Cash"`) will throw a `JsonException` that propagates to the global handler → 500 with `DontCatchExceptions()`. Fix: `c.Serializer.Options.Converters.Add(new JsonStringEnumConverter())` in the `app.UseFastEndpoints(...)` lambda in `Program.cs`. EF Core's `HaveConversion<string>()` convention is DB-side only and does not affect HTTP deserialization.
- **DontCatchExceptions() breaks FastEndpoints validator 400 responses (WI-09 round-2)**: When an endpoint calls `DontCatchExceptions()` in `Configure()`, FastEndpoints' own exception catch is disabled for ALL exceptions including `ValidationFailureException`. If a registered `Validator<T>` fails, FE throws `ValidationFailureException` BEFORE `HandleAsync` runs, but with `DontCatchExceptions()` active that exception propagates to the global handler → 500. The fix (in `Common/ValidationFailureExceptionHandler.cs`) is a global `IExceptionHandler` registered before `AddProblemDetails()` in `Program.cs` that intercepts `ValidationFailureException` and writes a 400 ProblemDetails response. This is required for ALL endpoints in this codebase because `DontCatchExceptions()` is mandatory per `rules/api-design.md`. Any new `Validator<T>` class is automatically protected; the handler is already in place.
- **App-minted JWTs vs real-clock validation (WI-05)**: `JwtIssuer` mints access tokens via the injected `TimeProvider` (FakeTimeProvider in tests, pinned to 2026-09-10 12:00 UTC). JwtBearer validates token lifetime using the real `DateTime.UtcNow`. A login-response access token expires at 12:15 fake-UTC — effectively unusable in test follow-up requests at any real time outside that 15-minute window. Rule: in integration tests, assert on the login response by decoding claims (never by replaying the access token against the API). Authenticate follow-up HTTP calls via `AuthHelpers` which mints with `DateTime.UtcNow` + 1-hour expiry. WI-15's E2E acceptance test (real `POST /auth/staff/login` → use token) will need a workaround: either derive a factory that pins FakeTimeProvider to the test-start real time, or configure a `LifetimeValidator` delegate wired to the injected TimeProvider.

- **JsonDocument projection in EF Core LINQ queries (WI-10)**: Columns mapped as `jsonb` (e.g., `OrderEvent.Payload`, `Zone.Polygon`, `AuditLog.Diff`) use `System.Text.Json.JsonDocument?`. You cannot project `Payload.RootElement.Clone()` (or any `JsonDocument` member) inside a LINQ-to-SQL `Select()` — EF Core cannot translate it and throws at runtime, surfacing as HTTP 500 via `DontCatchExceptions()`. Pattern: load with `ToListAsync()` first, then do the projection/mapping in memory. This applies to any `Select()` that touches a `JsonDocument` property.

- **XML doc comments on positional record constructor parameters (WI-11)**: In positional record declarations (`public record Foo(int X, string Y)`), the parameters are NOT valid targets for `/// <summary>` XML doc comments. Adding them causes CS1587 ("XML comment not placed on a valid language element") which fails `-warnaserror`. Only add `/// <summary>` on the record class itself. If per-parameter docs are needed, use a property-based record instead (`public record Foo { public int X { get; init; } ... }`).

- **`required` on STJ-deserialized request properties (WI-11)**: Using C# `required` on a `record` property (e.g., `public required Guid VehicleId { get; init; }`) causes STJ to throw `JsonException` when the field is missing from the JSON body. With `DontCatchExceptions()`, this propagates to the global handler → 500 instead of 400. For request DTOs that have validators, use plain `public Guid VehicleId { get; init; }` (defaults to `Guid.Empty`) and let `NotEmpty().WithErrorCode(...)` in the validator catch the missing value as a 400.

- **SignalR hub tenant scope (WI-13)**: `TenantResolutionMiddleware` only runs on the SignalR negotiate HTTP request, not on hub method invocations. Each hub method invocation gets a fresh DI scope where `CurrentTenant.FleetId` is null. Fix: inject `CurrentTenant` (concrete class) into `FleetHub` via the primary constructor and call `currentTenant.FleetId = fleetId` at the start of each hub method that queries via global query filters. For `Subscribe` (customer JWT — no fleet_id claim), use `IgnoreQueryFilters()` with `o.CustomerUserId == sub` as the tenant-safe predicate.
- **SignalR test transport (WI-13)**: `factory.Server.CreateHandler()` supports HTTP but not WebSockets. Use `HttpTransportType.LongPolling` in test `HubConnectionBuilder` options; `AccessTokenProvider` sends `Authorization: Bearer ...` header which is validated by `JwtBearer` directly (the query-string `access_token` path in `OnMessageReceived` is for production WebSocket connections only).
- **IRealtimePublisher.DriverStatusChangedAsync signature (WI-13)**: The signature was changed to include `Guid fleetId` as the second parameter so the SignalR publisher can route to the correct `fleet:{fleetId}:dispatch` group. All callers must pass `driver.FleetId` or `order.FleetId`. Update `NoOpRealtimePublisher`, `RecordingRealtimePublisher`, `GoOnlineEndpoint`, `GoOfflineEndpoint`, and `OrderService` when this signature changes.
- **TaxiApiFactory.FakeTime exposed (WI-13)**: `FakeTimeProvider` is created in the `TaxiApiFactory` constructor and exposed as `public FakeTimeProvider FakeTime`. Tests that advance time (e.g. throttle tests) must create their own `new TaxiApiFactory(fixture.ConnectionString)` rather than using the shared `PostgresFixture.Factory` because advancing time is irreversible and would contaminate other tests.

- **Background job testability pattern (WI-14)**: Each job (`OfferTimeoutJob`, `StalePositionJob`) exposes an `internal RunTickAsync(CancellationToken)` method with all per-tick business logic. `ExecuteAsync` is a thin `PeriodicTimer → await RunTickAsync` shell. Integration tests construct job instances manually and call `RunTickAsync` directly — deterministic, no timer waits, no need to advance `FakeTimeProvider`.

- **OfferTimeoutJob tenant context (WI-14)**: `OrderService.TransitionAsync` loads orders through the EF query filter (requires non-null `CurrentTenant.FleetId`). The job scans in one scope with `IgnoreQueryFilters`, projects `(OrderId, FleetId)`, then per-order creates a fresh scope, resolves `CurrentTenant` concrete, sets `tenant.FleetId = order.FleetId`, and resolves `OrderService` — so the transition runs in the correct tenant context. One scope per order prevents a poisoned change-tracker from a StaleVersion failure leaking into the next order.

- **OfferTimeoutJob FleetSettings query (WI-14)**: To get per-fleet `OfferTimeoutSeconds`, project `(int?)scanDb.FleetSettings.IgnoreQueryFilters().Where(fs => fs.FleetId == o.FleetId).Select(fs => (int?)fs.OfferTimeoutSeconds).FirstOrDefault()`. The inner `(int?)` cast is mandatory — without it, `FirstOrDefault()` on an `int` subquery returns `0` (not null) when no row exists, causing every fleet without a FleetSettings row to use a 0-second timeout and time out all Assigned orders immediately.

- **StalePositionJob null LastPositionAt handling (WI-14)**: Drivers with `LastPositionAt == null` who have an open shift older than 5 min are also marked stale. Without this clause, a driver who goes online and never reports a position would never be auto-offllined. Design decision: mid-ride drivers (EnRoute/Busy) are marked Offline per §9 of the spec ("status != Offline") — this does NOT release the active order; the dispatcher must reassign. Auto-offlining mid-ride is a dispatcher problem per the SKILL.md commentary.

- **DriverPositionStore.GetFleetId dead code removed (WI-14)**: `DriverPositionStore.GetFleetId()` and the `FleetId` field in `DriverState` were dead after WI-13 (StalePositionJob reads from DB directly, not the in-memory store). Both were removed in WI-14 to eliminate dead code, along with the `fleetId` parameter on `TryUpdate` (the FleetHub call site was updated to match).

- **FleetHub.FlushPositionToDatabaseAsync (WI-14 carry-forward)**: Fixed to use `CancellationToken.None` on both `FirstOrDefaultAsync` and `SaveChangesAsync`. Constraint: the flush must complete even when the client's `ConnectionAborted` token fires (connection dropped mid-flight). Changed flush log from `LogInformation` to `LogDebug` (per-driver position writes are high-frequency noise in prod logs).

- **WI-15 E2E clock-trap workaround**: `E2ETaxiApiFactory` extends `TaxiApiFactory` (which must NOT be `sealed`) and registers a `PostConfigure<JwtBearerOptions>` `LifetimeValidator` that validates token lifetime against `FakeTime` rather than the real wall clock. This allows tokens minted by `JwtIssuer` (using pinned FakeTimeProvider) to be accepted by JwtBearer in integration tests. Do NOT advance `FakeTime` in the E2E factory — advancing time would age seeded orders and trigger background jobs against the shared DB.

- **Seed:Enabled flag (WI-15)**: `DevelopmentSeeder` is always registered in DI when `IsDevelopment()`. Automatic startup seeding is gated by `Seed:Enabled` config (default `true`). `TaxiApiFactory` sets `Seed:Enabled=false` to prevent auto-seeding in tests. Tests call `DevelopmentSeeder.SeedAsync()` directly via scope resolution for controlled seeding. The seeder is idempotent — keyed on fleet slug `demo` existing.

- **OrderEvents secondary sort by Id (WI-15)**: `GetOrderEventsEndpoint` sorts by `At` then `Id` (UUIDv7). Without the `ThenBy(e => e.Id)` tiebreaker, events with identical timestamps (all fake-clock events) are returned in arbitrary DB order, causing non-deterministic event sequence in tests.

- **TaxiApiFactory no longer sealed (WI-15)**: Changed from `sealed` to allow `E2ETaxiApiFactory` to extend it for the clock-trap workaround. `ConfigureWebHost` is overridden in the subclass with a `base.ConfigureWebHost(builder)` call first.

- **FastEndpoints double query param binding is locale-sensitive (A1)**: FastEndpoints does NOT use `CultureInfo.InvariantCulture` when binding `double` properties from GET query strings. On Czech locale (cs-CZ), `50.08` (dot-decimal) fails to parse with "Value [50.08] is not valid for a [Double] property!". Pattern: declare request DTO properties as `string?` and parse manually with `double.Parse(s, NumberStyles.Float, CultureInfo.InvariantCulture)` in `HandleAsync`. Add a FluentValidation rule with `.Must(s => double.TryParse(s, NumberStyles.Float, CultureInfo.InvariantCulture, out _))` to return a clean 400 for invalid input.

- **Playwright chromium version mismatch (laneB11)**: `@playwright/test 1.48.2` requires `chromium-1140` but the pre-fetched browser was `chromium-1243`. Run `cd web && npx playwright install chromium` once to download the pinned revision. The correct binary is then at `chromium-1140/chrome-win/chrome.exe`.
- **vitest picks up Playwright spec files (laneB11)**: Without an explicit `exclude`, vitest v3 discovers `e2e/**/*.spec.ts` and fails with "Two different versions of @playwright/test" and `TypeError: test.describe.serial is not a function`. Fix: add `exclude: ['**/node_modules/**', '**/e2e/**']` to `vitest.config.ts` `test` options.
- **CreateOrderResponse shape mismatch (laneB11)**: `client.ts` originally typed `CreateOrderResponse` as `{ id: string }` but the API (`CreateOrderEndpoint`) returns `{ order: OrderDetailDto }`. This silently broke the B4 new-order highlight (orderId was `undefined`). The canonical fix is in `client.ts` + `useCreateOrder.ts` (use `data.order.id`).
- **Playwright test-results are volatile (laneB11)**: `test-results/` and `playwright-report/` change on every run. Add both to `.gitignore`. Do not stage `web/test-results/.last-run.json`.

- **vitest-axe in jsdom needs the configured helper (2026-09-12)**: raw `axe()` from `vitest-axe` runs the `color-contrast` rule, which calls `HTMLCanvasElement.getContext` — unimplemented in jsdom, producing a noisy stderr error on every a11y test. Always import `axe` from `src/shared/test/axe.ts` (a `configureAxe` wrapper with `color-contrast` disabled). Matchers are wired in `src/test-setup.ts`; assertion types come from `src/vitest-axe.d.ts` (the generic `Assertion<T>` param there needs an eslint-disable for `no-unused-vars`).
- **size-limit must stay on the v11 line (2026-09-12)**: `size-limit@12+`/`13` require Node ≥22.18, incompatible with the repo's Node 20.0.0 pin. Use `size-limit@^11.2.0` + `@size-limit/file@^11.2.0` (engines `^18 || >=20`). `npm run size` measures `dist/assets/*.js` (brotli) against the `"size-limit"` budget in `web/package.json` and needs a fresh `npm run build` first.
- **web/src/vite-env.d.ts was missing (2026-09-12)**: the project scaffold never had the standard Vite client-types reference, so any use of `import.meta.env` fails tsc with TS2339. The file now exists (`/// <reference types="vite/client" />`) — don't delete it.

- **EF Core `Database.SqlQuery<T>` maps result columns by snake_case, not the record property name (UC-007 A2)**: `SqlQuery<T>` (and `FromSql` onto a keyless type) binds each result column to the target property by the property's *configured* column name — and the project's snake_case naming convention applies. A positional record `DriverDayAggregate(int CardCzk, ...)` therefore expects a result column literally named `card_czk`, NOT `CardCzk`. Aliasing the SQL column `AS "CardCzk"` throws at runtime (surfaced as HTTP 500 via `DontCatchExceptions()`): `InvalidOperationException: The required column 'card_czk' was not present in the results of a 'FromSql' operation`. Fix: alias every projected column in snake_case — `COUNT(...) AS "rides_completed"`, `SUM(...) AS "card_czk"`, `(created_at AT TIME ZONE 'Europe/Prague')::date AS "day"`. The Prague-local day GROUP BY (`(created_at AT TIME ZONE 'Europe/Prague')::date`) translates under Npgsql 10; `EF.Functions.AtTimeZone` does NOT exist in this provider version (CS1061) — use raw `AT TIME ZONE` inside a `SqlQuery`/`FromSql`. The `.Concat()` → UNION-ALL + `OrderByDescending().ThenByDescending().Skip().Take()` audit-merge shape DOES translate in plain LINQ (no FromSql needed). Both shapes are pinned by `api/tests/Taxi.Api.Tests/Reports/TranslationSpikeTests.cs`.

- **zustand object-selectors loop at runtime, invisible to mocked tests (laneB3c)**: `useSomeStore(s => ({ a: s.a, b: s.b }))` returns a fresh object every render → zustand re-renders → infinite loop in a mounted component. Unit tests that `vi.mock` the store wholesale never exercise this, so it passes tests but crashes the real screen. Always use atomic one-field selectors (`const a = useStore(s => s.a)`), matching the `DriverHomePage`/`useOfferStore` precedent. Found in the partial `useRideRestore`/`useRideTransition` and fixed.

- **SMS tracking-token length is an SMS-budget constraint, not just security (UC-005 laneA5)**: a GSM-7 SMS is 160 chars; the OrderCreated template embeds the tracking link, and the link's dominant cost is the token. The original `TrackingTokenService` token (`Base64Url(UTF8("{exp}.{sig}"))` with a full 256-bit HMAC) was ~72 chars → the real SMS with a 30-char fleet name was 181–183 chars (two segments), even though the AC#5 test passed with a FAKE short token. Fix: the token is now `{exp}.{sig}` (no outer base64 wrapper — the inner string is already URL-safe) with a 128-bit (16-byte) truncated sig → ~33 chars. Any SMS-length test MUST render through a REAL `TrackingTokenService.Mint` (use `TrackingLink.Build`, the shared pure helper) — a fake short token silently hides a >160 regression. Truncating/changing the token is round-trip-safe as long as `Mint` and `Validate` stay symmetric (tokens are ephemeral; nothing persisted).

- **NoShowTimer.tsx vs noShowTimer.ts case collision (laneB3c)**: the designer's files_touched named `NoShowTimer.tsx` alongside the pure `noShowTimer.ts`; on Windows these differ only in case → TS1261 (the documented priceBadge trap). Resolution: no `NoShowTimer.tsx` — the ticking 5-min timer is `noShowTimer.ts` (pure) + `useActiveOrder.ts` (ticks once/sec) and the no-show button is folded into `RideButton.tsx`. Before creating any `PascalCase.tsx`, check for an existing `camelCase.ts` of the same stem.

- **Leaflet must stay out of the eager /driver ride chunk (laneB3c)**: `RideMapStrip` uses `React.lazy(() => import('./RideMapInner'))` and is collapsed by default; `RideMapInner.tsx` is the ONLY leaflet/react-leaflet importer under `features/driver/` and builds to its own chunk (~1.6 kB). Do not statically `import 'leaflet'` anywhere in the ride eager path. In jsdom tests, mock `./RideMapInner` (no layout engine) — never mount a real `MapContainer`.

- **AC#6 restore precedence — server over IDB (laneB3c)**: the active-ride restore reconciles with `GET /drivers/me.activeOrderId` as the authority; IndexedDB is only for instant paint + the local `arrivedAt`. A restore that returns early when IDB is empty (never calling the server) VIOLATES AC#6 "rebuild from the server". `reconcileActiveRide` always fetches `/drivers/me` first.

- **Driver route toast is consumed in DriverHomePage via useRouteToast (laneB3c)**: F-04 (`driver.ride.reassigned`) and B-complete (`driver.complete.successOverlay`) both navigate `'/driver'` with `{ state: { toast: '<i18n key>' } }`. `useRouteToast` reads `location.state.toast`, shows it 2 s, then clears the history state (so refresh/back doesn't replay). Any new "return Home with a message" flow must pass a toast key this way and add the key to both locales.

- **Customer login calls MUST set `skipAuthRedirect: true` (laneB4a)**: `auth/customer/verify-code` returns **401 on a wrong code** and `request-code` returns 429 on rate-limit. Without `skipAuthRedirect`, client.ts's 401 branch either redirects to `/dispatcher/login` or (if silent refresh is enabled) clears storage — destroying the "Kód nesouhlasí" / rate-limit form errors. `requestCustomerCode` and `verifyCustomerCode` both pass `skipAuthRedirect: true` (mirrors `postStaffLogin`); the 401/429 then surfaces as an `ApiResponseError` the hook catches. Any new customer auth call follows this.

- **F-05 fleet-slug resolution + synchronous persist (laneB4a)**: the logged-out customer PWA has no JWT `fleet_slug` claim and (on localhost) no subdomain, so every `public/*` call 404s unless `authStorage.getFleetSlug()` is set first (client.ts auto-attaches `X-Fleet-Slug`). `resolveFleetSlug(hostname, search, pathname)` resolves in order: subdomain → `?fleet=` → `/customer/f/{slug}` path → `'demo'` default. `ensureFleetSlug()` (resolve + `authStorage.setFleetSlug`, newly added token-independent write) is called inside a **`useState` initializer** (runs during render, before any child query's fetch effect) — not `useEffect`, which would race the branding query. `authStorage.clear()` wipes the slug, so it is re-resolved on every mount. CRITICAL: `/customer/login` is a router **sibling** of `CustomerLayout`, NOT a child, so `CustomerLoginPage` must call `ensureFleetSlug()` itself — otherwise a direct hit to `/customer/login` (bookmark/refresh/E2E) fires `request-code` with no `X-Fleet-Slug` and 404s on localhost. Mocked-client login tests can't catch a missing slug; `CustomerLayout.test.tsx` + `ensureFleetSlug.test.ts` guard it.

- **Inline-login surface = onAuthenticated continuation, no baked navigation (laneB4a)**: `useCustomerLogin({ onAuthenticated })` and `CustomerLoginStep` never call `navigate()` themselves (unlike `useDriverLogin`). Standalone `/customer/login` (`CustomerLoginPage`) passes `() => navigate('/customer')`; the order WIs (B-route-order/B-custom-order) pass a resume-create callback so the parent keeps form state and the login runs inline without losing the order. On verify success the hook stores tokens via `authStorage.setTokens` + `idbAuthStore.setRefreshToken`, then `enableSilentRefresh('/customer/login')` + `scheduleProactiveRefresh` (never re-ask on device unless refresh fails).

- **client.ts double-401 `_isRetry` branch hardcodes `/driver/login` (laneB4a, known edge)**: the customer *terminal* refresh failure correctly uses refresh.ts's configurable `failureRedirectPath` (`/customer/login`). But the retried-request-got-401 edge (client.ts lines ~80-84, gated on `isSilentRefreshEnabled()`) redirects to `/driver/login` regardless of role. Not hit by the customer happy path or these ACs (login calls use `skipAuthRedirect`); left as-is to keep driver tests green. Generalize later if a customer ever hits it.

- **CustomerLayout nests a ThemeProvider for branding (laneB4a)**: `useFleetBranding` maps `GET public/fleet.primaryColorHex` (`#RRGGBB`, else falls back to `theme.colors.primary`) onto a theme override via `applyFleetBranding`, and `CustomerLayout` wraps its subtree in a second `styled-components` `ThemeProvider` (nested providers override). The root provider in `app/providers.tsx` is unchanged.

- **Single PWA manifest covers the whole app (laneB4a)**: vite-plugin-pwa allows only one manifest/`start_url`; the existing `/driver` config (scope `'/'`, globs `**/*.{js,...}`) already precaches the lazy `/customer` chunks, so no vite.config.ts change was needed for `/customer` installability. Limitation: the installed app name/start_url are the driver's (`start_url: '/driver'`). `devOptions.enabled` stays false and no runtime cache matches `^/api`/`^/hubs` — Playwright/E2E untouched.

- **`WithTag(_featureConfiguration)` needs `using Taxi.Api.Common.Features;` (UC-004 laneA4)**: The `Description(b => b.WithName(...).WithTag(_featureConfiguration))` extension is an extension method in the `Taxi.Api.Common.Features` namespace, NOT in FastEndpoints. A new endpoint file that omits that `using` fails with CS1061 "RouteHandlerBuilder does not contain WithTag". Every new endpoint must import `Taxi.Api.Common.Features` (in addition to `FastEndpoints`).

- **FakeGeoProvider is a shared singleton across the whole test collection (UC-004 laneA4)**: `TaxiApiFactory` registers ONE `FakeGeoProvider` for the shared `PostgresFixture.Factory`, so `RouteShouldThrow`/`SuggestShouldThrow`/`RouteResult` set by one test leak into every other test in the `[Collection(TestCollections.Database)]` (xUnit does not guarantee intra-collection ordering). Any test helper that configures the fake MUST call `fake.Reset()` FIRST, then set the field — otherwise a lingering `RouteShouldThrow=true` from an earlier "OSRM-down" test turns a later happy-path 200 into a 502, and the failure only appears in the full-suite run (not in `--filter` isolation). Pattern: `var fake = ...GetRequiredService<FakeGeoProvider>(); fake.Reset(); fake.RouteResult = ...;`.

- **OpenAPI route-allowlist test gates every new endpoint (UC-004 laneA4)**: `SeedAndEndToEndTests.OpenApi_Document_ListsAllExpectedEndpoints` keeps an explicit `expected` HashSet of every `METHOD /api/v1/...` route and fails (via `extra.Should().BeEmpty()`) the instant a new endpoint appears in the OpenAPI doc. Any new endpoint (and `{route:constraints}` render as `{route}`) must be added to that allowlist. A sibling test regenerates `docs/api.md` from the live OpenAPI doc — stage the updated `docs/api.md` after adding endpoints.

- **OrderDetailDto rating fields are trailing-optional (UC-004 A-rating, F-02)**: `OrderDetailDto` gained `int? RatingStars = null, string? RatingComment = null, DateTimeOffset? RatedAt = null` appended to the positional record. Because they are trailing optionals, all ~13 construction sites (every transition endpoint via `OrderDetailMapper.ToDto`, `CreateOrderEndpoint.ToDetailDto`, `GetOrderEndpoint`) compile UNCHANGED. Only `GetOrderEndpoint` (dispatcher detail, AC #7) actually populates them; the mapper and create leave them defaulted. Adding trailing params to a positional record whose other params have `<param>` docs REQUIRES matching `<param name="RatingStars">` tags or it is CS1573 under `-warnaserror`.

- **vi.mock with async factory for React components in chart tests (WI-13)**: When mocking `react-chartjs-2` in a test that also imports React, the standard `vi.mock('react-chartjs-2', chartMockModule)` pattern causes a TDZ error because `vi.mock` is hoisted before the `chartMockModule` import resolves. Fix: use an async factory `vi.mock('react-chartjs-2', async () => { const { createElement } = await import('react'); ... })`. The shared `chartMock.ts` `chartMockModule` can still be used when the test file does NOT reference `chartMockModule` via a synchronous import; if it does, use the async inline factory pattern.

- **chartMock.ts stubs need `role="img"` (WI-13)**: Axe's `aria-prohibited-attr` rule fires when a `<div>` has an `aria-label` attribute but no valid ARIA role. The chart stubs in `chartMock.ts` (and inline test mocks) must render with `role="img"` so axe is satisfied. Updated in WI-13.

- **Vite `manualChunks` required for analytics chunk naming (WI-13)**: Without a `manualChunks` config, Rollup names the lazy analytics chunk after its entry module (`AnalyticsPage-[hash].js`), which the size-limit glob `dist/assets/analytics-*.js` does NOT match. Add `build.rollupOptions.output.manualChunks(id)` returning `'analytics'` for any id containing `/features/analytics/`, `/chart.js/`, or `/react-chartjs-2/`. This is in `web/vite.config.ts`.

- **`DateRange` from analyticsRange.ts holds ISO strings, not `Date` objects (WI-13)**: `DateRange.from` and `DateRange.to` are `string` (yyyy-MM-dd). Calling `.toISOString()` on them fails at runtime. Use the string directly: `from` is already in `yyyy-MM-dd` format for the API.

- **`percentile_cont` returns NULL on empty set — SqlQuery record must use nullable double (WI-06)**: When no orders qualify for an SLA metric (both interval endpoints must be non-null), PostgreSQL's `percentile_cont(...)` returns NULL for both p50 and p90 even though the COUNT is 0. The internal `SlaRow` record MUST use `double? Median` and `double? P90` (nullable). Using non-nullable `double` causes EF Core to throw at runtime when mapping DB NULL to the property, surfacing as HTTP 500 with `DontCatchExceptions()`. Check SampleCount == 0 *after* safely mapping the row (the null-coalesce in ToSlaDto handles it). This applies to any `SqlQuery<T>` record that maps aggregate result columns that can be NULL when the input set is empty.

- **`FormattableStringFactory.Create` with `{0}` placeholders is incompatible with raw string literals `$"""..."""` (UC-009 WI-07)**: Using `$"""..."""` raw string literal syntax with `{0}`/`{1}`/`{2}` positional placeholders and `FormattableStringFactory.Create` causes CS9006 ("The interpolated string does not have enough $ characters"). The compiler tries to interpret `{0}` as an interpolation hole rather than a literal `{0}` to be passed to `FormattableStringFactory`. Fix: use concatenated regular strings — `$"SELECT date_trunc('{truncUnit}', ...) " + " FROM table WHERE col = {0} " + ...` — then `FormattableStringFactory.Create(sql, param0, param1, ...)`. This pattern (from `GetOverviewEndpoint`) is required for all bucketed queries where `truncUnit` is embedded as a string literal while `fleetId`/`winStart`/`winEnd` are SQL parameters. Fixed-filter queries (no dynamic truncUnit) can use raw string literals directly with interpolated parameters.

- **Zone bbox helper promotion to `Common/Geo/ZoneBbox.cs` (UC-009 WI-07)**: When two or more features need the same zone bounding-box computation (WI-05 demand + WI-07 revenue), promote the private method to a public static class under `Common/Geo/` per `architecture.md#common-infrastructure`. `ZoneBbox.Compute(Zone zone)` handles Circle (cos-corrected lng delta) and Polygon (vertex min/max) shapes; malformed zones return an invalid box `(1, 0, 1, 0)` that callers detect and skip. Do NOT copy the private method to each endpoint — the rules say "do NOT duplicate it" and architecture permits `Common/` for code reused by 2+ features.

- **PostgreSQL MAX(uuid) does not exist (UC-009 WI-09)**: `MAX()` aggregate is not defined for the `uuid` type in PostgreSQL. Using `MAX(customer_user_id)` in a `SqlQuery<T>` call throws at runtime: `42883: function max(uuid) does not exist`. Workaround: `MAX(customer_user_id::text)::uuid` — cast to `text` first, then cast the result back to `uuid`. This applies to any query needing to reduce a UUID column with a set function (`MIN`, `MAX`, etc.).

- **The e2e harness seeds only `DevelopmentSeeder`, NOT the 50k `ReportSeedScript` (UC-009 WI-17)**: `Program.cs` startup seeding (gated by `Seed:Enabled`) runs `DevelopmentSeeder.SeedAsync()` only. `ReportSeedScript` (the 50k analytics perf dataset) is registered in DI but is invoked ONLY directly by API integration tests — it never runs at startup, so the Playwright `webServer` harness has NO 50k data. Analytics e2e assertions must anchor on `DevelopmentSeeder`'s handful of demo orders: DEMO05 is the one Completed ride (180 CZK, driver1, anchored at `now()`), which is what guarantees a non-empty Overview series and rides≥1 in the current-month/7-day range. Keep numeric assertions minimal (chart `role="img"` present / KPI label visible / not-error, not exact values). The drivers league (`GetDriversEndpoint` step 5 loads all active fleet drivers) and `admin/analytics` (lists all active fleets) both return structurally non-empty tables regardless of activity. `web/e2e/analytics.spec.ts` is the reference.

- **SuperAdmin IS reachable in e2e via the A7b login path (UC-009 WI-17)**: the `PlatformScreen.tsx`/`AdminFleetsPage.tsx` "reaching /admin needs a SuperAdmin JWT the backend cannot mint" comment is STALE. `onboarding.spec.ts` and `analytics.spec.ts` both prove it: a `beforeAll` runs the `create-superadmin` CLI (`dotnet run --no-build ... create-superadmin --email ... --password ...`, idempotent) to insert a fleetless SuperAdmin, then `POST /auth/admin/login` (the exact endpoint the `/admin/login` UI form posts to) returns a `role=SuperAdmin` token with no `fleet_id` claim. For a UI e2e, log in through `/admin/login` (`#admin-email`/`#admin-password`) then reach `/admin/platform` by CLICKING the in-app `PlatformLink` on `/admin` (client-side nav) rather than a full `page.goto('/admin/platform')`.

- **Polly v8 resilience API (UC-010 WI-04)**: `AddResilienceHandler` lambda receives `ResiliencePipelineBuilder<HttpResponseMessage>`. Strategy method names are in the `Polly` namespace — use `AddRetry(RetryStrategyOptions<HttpResponseMessage>)`, `AddCircuitBreaker(CircuitBreakerStrategyOptions<HttpResponseMessage>)`, `AddTimeout(TimeoutStrategyOptions)`. Using `HttpRetryStrategyOptions`/`HttpCircuitBreakerStrategyOptions`/`HttpTimeoutStrategyOptions` from `Microsoft.Extensions.Http.Resilience` namespace does NOT work for the `AddResilienceHandler` pipeline builder (those are for `AddStandardResilienceHandler`). `AddResilienceHandler` returns `IHttpResiliencePipelineBuilder`, not `IHttpClientBuilder` — call it without assigning, then return the original `builder`. When using `BrokenCircuitException` in a pattern match inside `ShouldHandle`, import from `Polly.CircuitBreaker` namespace.

- **Czech locale trap for JSON construction in tests (UC-010 WI-04)**: C# string interpolation uses the current culture for `double.ToString()`. On the Czech locale (cs-CZ), `$"{50.001}"` produces `"50,001"` (comma decimal separator), which is invalid JSON. Test code that builds JSON strings containing doubles MUST use `CultureInfo.InvariantCulture` explicitly: `$"{value.ToString(CultureInfo.InvariantCulture)}"`. This is distinct from the FastEndpoints query-binding locale trap (which affects HTTP binding); the JSON trap affects any in-memory test JSON construction.

- **GeoService fleetless-suggest bypass (UC-010 WI-07)**: `geo_cache` and `geo_usage` both have FK constraints to the `fleets` table. `GeoService.SuggestAsync` accepts `fleetId = Guid.Empty` for fleetless customers (no `fleet_id` JWT claim). Calling `GeoCache.GetOrAddAsync` with `Guid.Empty` causes EF to throw `InvalidOperationException` ("value of FleetId is unknown when attempting to save changes") before even reaching Postgres — there is no real FK row for the empty GUID. Fix: in `GeoService.SuggestAsync`, when `fleetId == Guid.Empty`, call `mapyClient.SuggestAsync` directly and return `new GeoCacheResult<...>(directResult, WasHit: false)` — bypasses cache+usage entirely. Suggest results are not tenant-sensitive so this is safe. All other GeoService methods (Geocode, Reverse, Route, QuickPlace) require a real fleet context; callers guard appropriately.

- **FakeGeoService.RouteResult type (UC-010 WI-07)**: `MapyRouteResultData(distanceMeters, durationSeconds, [])` — not the old `GeoRouteResult`. The `[]` is the waypoints list (third parameter, always empty for test fakes).

- **GeoRateLimiter singleton contamination (UC-010 WI-08)**: `GeoRateLimiter` is registered as `AddSingleton<GeoRateLimiter>()` in `GeoFeatureConfiguration`. Its token buckets persist across all tests that share the same `TaxiApiFactory` instance. Tests that exhaust the 5-req/s bucket (e.g. rate-limit tests) MUST create a dedicated `new TaxiApiFactory(fixture.ConnectionString)` — not use the shared `PostgresFixture.Factory` — to avoid depleting buckets and causing spurious 429 responses in unrelated tests. For tests that merely check normal behavior, the natural isolation comes from each test minting a fresh `Guid.CreateVersion7()` userId which gets its own bucket automatically. `GeoRateLimiter.Reset()` clears all buckets and is available if needed in custom-factory tests.

- **GeoRateLimiter user ID source (UC-010 WI-08)**: All three geo proxy endpoints (Suggest, Geocode, Reverse) read the user ID for rate-limit keying via `User.FindFirst("sub")?.Value` (mirroring `LogoutEndpoint`). When the sub claim is absent or unparseable as Guid, `TryAcquire` is not called and the request is rate-limited immediately (returns 429). This is intentional — an authenticated request without a parseable sub is anomalous.

- **Verb change on geo/route breaks three hardcoded test files (UC-010 WI-09)**: Changing the HTTP verb of any endpoint (e.g. GET→POST on `geo/route`) silently breaks ALL test files that have hardcoded `GetAsync(url)` calls — including `GeoProxyTests.cs`, `RouteAccessTests.cs`, AND `SuggestAccessTests.cs`. Failing tests return 405 Method Not Allowed, not a build error. Before any future verb change: `grep -r "GetAsync\|PostAsync" api/tests --include="*.cs" | grep <route>` to find every caller, then update each one before running the full suite.

- **MapyConstants.cs tile URL and attribution are best-effort (UC-010 WI-09)**: `TileUrlTemplate` (`https://api.mapy.cz/v1/maptiles/basic/256/{z}/{x}/{y}?apikey={apikey}`) and `AttributionHtml` in `Common/Geo/MapyConstants.cs` were sourced from general Mapy.com knowledge — the repo's spec/decisions/MapyClient files contain no tile URL. The API integration tests only assert `Contains("mapy.cz")` and `NotBeNullOrEmpty()`, so they pass even with a wrong URL. WI-15/16 (web lane) MUST verify the exact tile path, `{apikey}` substitution, `@2x` variant, and mandatory attribution text against the live Mapy.com API docs before the frontend uses them.

- **Shared MapyMap owns the map, not each *Inner (UC-010 WI-15)**: `web/src/shared/map/MapyMap.tsx` is the ONE MapContainer+Mapy-TileLayer+attribution wrapper; all 6 map components (`MapPanel`, `TrackingMapInner`, `PickupMapInner`, `RideMapInner`, `PinPickerMap`, `ZoneEditorMap`) render `<MapyMap>...children...</MapyMap>` and pass Markers/`useMap`/`useMapEvents` controllers as CHILDREN (children render inside MapContainer so the react-leaflet context is preserved). MapyMap consumes `useGeoConfig()` (['geo','config']) internally and defaults center/zoom from config when the caller omits them; callers with a concrete location pass `center`. CRITICAL: react-leaflet reads center/zoom only at MOUNT — MapyMap therefore does NOT mount MapContainer until `useGeoConfig` resolves (renders `t('map.loading')` role=status meanwhile), else config-defaulted maps mount on `undefined` center and stick. Leaflet-lazy discipline is intact: MapyMap statically imports react-leaflet+leaflet but is only imported inside the lazy `*Inner`/settings-map chunks — verified in dist (leaflet lands only in PickupMapInner/PinPickerMap/RideMapInner/TrackingMapInner/ZoneEditorMap lazy chunks + the pre-existing eager `index` where the board already carried it). MapyMap imports `./leafletSetup` for its icon-fix side effect (that side effect used to piggyback on the OSM_* imports the components dropped). Unit-test MapyMap by mocking `react-leaflet` (MapContainer→stub that renders children + exposes center/zoom as data-attrs; TileLayer→stub exposing url/attribution), mocking `./useGeoConfig`, and mocking `./leafletSetup`→`{}` — never mount a real MapContainer in jsdom.

- **WI-15 filename collided with a stale UC-009 analytics handoff (UC-010 WI-15)**: `.claude/state/handoff-frontend-developer-WI-15.json` already existed from a DIFFERENT feature (UC-009 analytics Drivers/Customers tabs). Handoff filenames are keyed on `WI-<n>` only, so WI numbers repeat across UCs and collide. The UC-010 WI-15 handoff overwrote it. If historical analytics WI-15 detail is needed, recover from git history, not this file.

- **Forbidden-host CI guard must live OUTSIDE its own scan roots (UC-010 WI-18)**: the AC#1 guard greps `web/src`+`api/src` for the 4 legacy map hosts (`router.project-osrm.org`, `nominatim.openstreetmap.org`, `photon.komoot.io`, `tile.openstreetmap.org`). If the scanner (or its test) holds those literals inside `web/src`, it matches itself and can NEVER go green. Fix: keep `FORBIDDEN_HOSTS` + the scan function in `web/scripts/check-no-osm.mjs` (`web/scripts` is not a scan root) and have the vitest guard (`web/src/shared/map/noLegacyMapHosts.test.ts`) IMPORT the list from there — zero forbidden literals in `web/src`. Plant the positive-catch fixture in `os.tmpdir()`, never under `web/src`. Wired: `npm run check:geo` (standalone) + `lint` runs `check:geo &&` eslint + the vitest guard runs in `npm test`. eslint `no-restricted-syntax` was rejected because eslint only lints `web`, not `api/src`.

- **A strict-tsc test importing a `.mjs` needs a sibling `.d.mts` (UC-010 WI-18)**: `web/src/shared/map/noLegacyMapHosts.test.ts` imports `../../../scripts/check-no-osm.mjs`; under `tsc --noEmit` (moduleResolution bundler, strict), a bare `.mjs` import throws TS7016 "implicitly has an 'any' type". Fix: add `web/scripts/check-no-osm.d.mts` next to the `.mjs` (matching stem) declaring the exported members — TS resolves `.mjs` → sibling `.d.mts` even though `scripts/` is outside tsconfig `include`.

- **TaxiApiFactory singleton IGeoService override masks captive-dependency DI bugs (UC-010 WI-11-fix)**: `TaxiApiFactory` registers `FakeGeoService` as a singleton (line `services.AddSingleton<IGeoService>(...)`). A singleton `PickupEtaService` that directly injects `IGeoService` in its ctor will NOT fail Development scope validation in the test host because the fake is singleton — the captive dep only surfaces when `IGeoService` is scoped (as it is in production). Regression tests for DI lifetime bugs MUST use a focused `ServiceCollection` with `AddScoped<IGeoService>` + `BuildServiceProvider(new ServiceProviderOptions { ValidateScopes=true, ValidateOnBuild=true })` — never a factory-based test. The fix pattern for a singleton that needs a scoped dep: inject `IServiceScopeFactory` instead and open a per-call `CreateAsyncScope()`. Mirror `OfferTimeoutJob`; update `BuildService` helpers in tests to pass a scope factory wrapping the fake as a singleton instance (so call-count tracking accumulates correctly across per-call scopes).

- **Bodyless driver-transition POSTs must NOT send `Content-Type: application/json` (UC-010 WI-19 e2e)**: the driver transition endpoints `POST /orders/{id}/{arrive,start}` take an empty body (only the route id). With `DontCatchExceptions()` mandatory on every endpoint, if the request carries `Content-Type: application/json` but an empty body, FastEndpoints tries to deserialize it and throws `System.Text.Json.JsonException: The input does not contain any JSON tokens` → HTTP 500 (not a clean 400). The proven `web/e2e/helpers/apiDriver.ts` `acceptOrder` helper omits Content-Type for exactly this reason. Rule for e2e/API-driver helpers: set `Content-Type: application/json` ONLY when a JSON body is actually sent (e.g. `complete`, `assign`, `cancel`); for bodyless transitions send only `Authorization` + `X-Fleet-Slug`. `accept`, `arrive`, `start` are bodyless.

- **Leaflet tile `<img>` ignores HTTP status — a keyless 401 does NOT trigger the degraded map banner in e2e (UC-010 WI-19)**: `MapUnavailableBanner` (`MapyMap.tsx`) appears via the TileLayer's `tileerror` handler flipping `tilesFailed`. But a tile `<img>` fires its `error` event only when the response BYTES fail to decode as an image — NOT on an HTTP 401/403/500 whose body is decodable/treated-as-loaded. In the keyless e2e harness, `api.mapy.cz/v1/maptiles/...?apikey=` returns 401 with a decodable body, so Leaflet emits `load` (not `tileerror`) and the banner never shows (observed: map container 599×667, 12 tiles → 401, `role=alert` count 0 after 3 s). To assert the degraded banner in Playwright, model a HARD tile failure: `await page.route(/api\.mapy\.cz\/v1\/maptiles\//, r => r.abort())` before opening the map — an aborted request fires the `<img>` error → `tileerror` → banner. This is the mirror of the AC#4 attribution test, which instead `route.fulfill`s the tiles with a valid PNG so they SUCCEED. Not a MapyMap bug (unit test `MapyMap.test.tsx` covers the synthetic-tileerror path; real hard failures fire it correctly).

- **i18n translation-completeness scans run over VALUES only, and per-locale diacritic sets differ (UC-011 WI-3…6)**: When verifying a `cs-CZ`-seeded locale JSON was actually translated (not left as placeholder Czech), the two completeness tools are a leftover-diacritic scan and a `<locale>==cs` equality scan — but BOTH must iterate leaf VALUES only, never keys. Several KEYS are legitimately Czech-looking and must stay byte-identical or the WI-1 parity gate REDs: `board.cancelReasons` keys `"zákazník zrušil"`/`"nedorazil"`/`"omyl"`/`"jiný"`, `analytics.ranges.*` (`dnes`,`7dni`,`tentoMesic`,…), `analytics.tabs.*` (`prehled`,`poptavka`,`trzby`,…), `driver.settings.diagnostika`. A key-inclusive diacritic scan false-positives on these and masks real misses. Legit value-side diacritic exceptions after a clean translation: `Kč` (currency, stays per `rules/web-react-style.md#dates-and-money`) and deliberately-kept Czech example proper nouns (`Kutná Hora`, `Nádražní 1, Kutná Hora`, `Centrum Kutná Hora` — proper nouns, not localized; uk-UA transliterated only because Cyrillic forced it, Latin-script fil/de keep the true spelling). CRITICAL for WI-6 (de-DE): German adds ä/ö/ü/ß which are NOT Czech-leftover markers — the diacritic scan's flag-set must be the Czech-specific glyphs (ě š č ř ž ý á í é ú ů ň ť ď ó and caps) MINUS any that overlap German, or every German umlaut false-positives and the scan is useless. The `<locale>==cs` equality scan catches what the Latin-script diacritic scan cannot (an English/Filipino word that happens to have no Czech diacritics but was never translated). Filipino naturally code-switches (keeps `password`, `driver`, `e-mail`, browser-verbs) so its equality-scan hit-list is longer but every hit must resolve to placeholder-only/brand/symbol/currency.

- **vitest-axe on a MapyMap-based component fails `aria-prohibited-attr` unless the mocked MapContainer carries a role (UC-014 WI-2)**: A component that renders `<MapyMap ariaLabel=...>` forwards the label to react-leaflet's `MapContainer`. In jsdom tests the `MapContainer` mock is a bare `<div aria-label=...>`, which axe flags as `aria-prohibited-attr` ("aria-label cannot be used on a div with no valid role"). Fix: give the mock stub a role — `MapContainer: ({children, ...rest}) => <div role="application" aria-label={rest['aria-label']}>{children}</div>` (same class of fix as the documented `chartMock.ts` `role="img"` trap). Note: `MapyMap.test.tsx`'s own axe test escapes this only because it renders MapyMap WITHOUT an `ariaLabel` in the axe case; any NEW map component that DOES pass an aria-label needs the role on its MapContainer mock. `web/src/features/customer/shell/CustomerMapBackground.test.tsx` is the reference.
- **theme.zIndex.attribution cannot lift the Mapy logo above overlay slots (UC-014 WI-2)**: `theme.zIndex` (added by WI-2: map<overlay<attribution<modal) is a Shell-level scale, but MapyMap renders the Mapy logo at `z-index:1000` INSIDE the map layer's own stacking context (the shell's `MapLayer` is `z-index:map=0`). So relative to the Shell the logo paints BELOW overlay slots (`z-index:overlay=100`), NOT above — raising `theme.zIndex.attribution` does nothing. Keeping the mandatory Mapy logo reachable (AC#4) is a LAYOUT problem: any opaque content in the bottom overlay slot (UC-015/016 sheets) must leave the bottom-left corner clear (inset/margin), not rely on z-index. The empty WI-2 bottom slot is `pointer-events:none` so it is click-through and the logo stays reachable.

- **`customer.tracking.reorder` did NOT pre-exist — only `customer.history.reorder` does (UC-016 WI-1)**: the UC-016 design-review finding F3 claimed `customer.tracking.reorder` ('Objednat znovu') already existed in cs-CZ.json and must be reused as-is; it does NOT. A grep of all 6 locales found only `customer.history.reorder`. Because WI-2's `files_touched` excludes the locale JSONs (all tracking i18n is front-loaded into WI-1), the tracking-namespace `reorder` key was ADDED fresh to all six locales in WI-1 rather than coupling WI-2 to the history feature's key across a feature boundary. Also added `customer.tracking.sheetLabel` for `BottomSheet`'s required `ariaLabelKey`. Lesson: before "reuse an existing i18n key" per a review finding, grep the actual `<feature>.<key>` path — a same-leaf key under a different namespace is a different key.

- **headlineRules `phase` name inversion (UC-016 WI-1)**: `deriveHeadline` now returns a `phase: TrackPhase` discriminant. Backend status **Assigned** collapses to phase **`searching`** (heading `customer.tracking.headline.new`, "Hledáme řidiče…") per spec §2 so the searching loader is never paired with a "driver on the way" heading — this CHANGED Assigned's heading from the old `assignedNoEta`. Backend status **Accepted** maps to phase **`assigned`** (keeps assignedEta/assignedNoEta). The status↔phase names are deliberately inverted (Assigned→searching, Accepted→assigned) — easy to transpose. Assigned still keeps `showCancel:true`. Unknown status → safe `searching` phase, cancel blocked.

- **Directly-seeded `OrderEvent` rows require all non-nullable columns or FK violation fires (UC-017 WI-2)**: When seeding `OrderEvent` objects directly in integration tests (bypassing `OrderService`), every non-nullable column MUST be populated: `FleetId` (FK → `fleets.id`; FK constraint name `fk_order_events_fleets_fleet_id`), `FromStatus` (`OrderStatus?`, the status before the transition), `ToStatus` (`OrderStatus`, the status after), and `ActorRole` (`UserRole`). Omitting any of these causes an EF `DbUpdateException` wrapping a Postgres FK or NOT-NULL violation, which surfaces as a misleading test failure. This is distinct from `ActorUserId` which IS nullable (null for `Actor.System`). See `AutoDispatchJobTests.cs` `RunTick_TimedOutDriverExcluded_NextNearestAssigned` and `RunTick_ManualAssignWinsRace_JobNoOp` for the corrected seed pattern.

- **Map-first driver UI has NO ConnectionDot — driver e2e hub-wait must anchor on the SignalR WebSocket (UC-019 WI-6)**: The UC-019 map-first `/driver` UI (`DriverMapScreen` + `DriverLayout`) renders NO `ConnectionDot`/connection-word (it exists in `features/driver/home/ConnectionDot.tsx` but is unmounted). The old `driver.spec.ts` `waitForHubConnected` polled `getByText('green')` from ConnectionDot — that now hangs forever. The dispatcher spec's "offline banner absent" check is ALSO wrong here (satisfied before any connect). Re-anchor on a POSITIVE, UI-independent signal: `page.waitForEvent('websocket', { predicate: ws => ws.url().includes('/hubs/fleet') })`, armed BEFORE the login navigation and awaited after `waitForURL('/driver')`. A positive wait is MANDATORY because a missed `NewOrderOffered` push is unrecoverable (`useOfferListener` has no catch-up; `useFleetHub` only writes the offer on the live event) — so the dispatcher must not assign before the hub connects. Harness uses a real WS transport (setOffline does not drop it).

- **Map-first driver /driver screen hydrates the active-order store on Accept (UC-019 WI-5 defect, surfaced by WI-6 e2e — FIXED WI-5-fix)**: The ONLY writer of `useActiveOrderStore.setOrder` in `web/src` was `useRideRestore.ts` (`useEffect` deps `[myDriverId]` → MOUNT-ONLY). In the old flow `OfferTakeover` navigated to `/driver/ride`, which MOUNTED `DriverRidePage` fresh → its `useRideRestore` ran → hydrated the store. WI-5 removed the `/driver/ride` route and made Accept navigation-less (the same `/driver` screen stays mounted) but did NOT replace the navigation-as-hydration-trigger → after Accept the store stayed null → `DriverMapScreen` rendered the Free panel, never `<RideSheet>` → "Jsem na místě" never appeared. WI-5's unit tests `vi.mock`'d `useActiveOrder` to RETURN an order, so they never exercised this path (the "mocked tests pass, real flow breaks" class). **Fix (seam):** `OfferCard.handleAccept`, on `outcome.type === 'success'`, fires `void reconcileActiveRide('').then(r => { if (r.outcome === 'active' && r.order) setOrder(r.order) })` BEFORE `applyDecision`/`onDismiss` — reuses the existing AC#6 server-authoritative reconcile (it fetches `GET /drivers/me` itself and ignores its `myDriverId` arg, so no `useDriverMe` lookup is needed in OfferCard). Fire-and-forget (NOT awaited) so the no-navigate + onDismiss contract is preserved. `setOrder` is an ATOMIC selector (`useActiveOrderStore(s => s.setOrder)` — laneB3c loop trap). `useRideRestore` mount-only behavior is UNCHANGED (extend, don't break). Pinned by `web/src/features/driver/offer/acceptHydration.test.tsx` which composes `<OfferCard>` + `<DriverMapScreen>` as siblings sharing the module-global store starting EMPTY, with REAL `useActiveOrder`/`useActiveOrderStore` (only accept + reconcile mocked), and asserts "Jsem na místě" appears after Accept. Any OfferCard test that clicks Accept must `vi.mock('../ride/useRideRestore', ...)` (it renders with only I18n+Theme, no QueryClient) or the real `getDriverMe` fetch fires.

- **`astro check` crashes with ERR_REQUIRE_ESM on the Node 20.0.0 pin — pin language-server to 2.15.4 (UC-022 WI-1)**: The repo Node pin is exactly `v20.0.0`, below the `>=20.19.0` floor where Node supports `require()` of an ESM module. The current `@astrojs/language-server` (2.17.1, resolved by ANY `@astrojs/check` `^0.8`/`^0.9` range) does a CommonJS `require('@astrojs/astro2tsx')` where `astro2tsx` is ESM-only → `astro check` (and `npm run check`) crashes with `Error [ERR_REQUIRE_ESM]` (exit 1) even though `npm run build` is green. `astro build` (Vite/esbuild) is unaffected — only the check/language-server path hits it. Fix in `product-website/package.json`: pin `@astrojs/check` to `0.8.3` AND add an `"overrides": { "@astrojs/language-server": "2.15.4" }` block. 2.15.4 is pre-split (bundles the tsx transform internally, no external ESM `require`), so `astro check` exits 0 on Node 20.0.0. This is dependency selection, NOT an engine/`.npmrc` hack — do NOT add `engine-strict`, do NOT touch the Node pin, keep `engines.node` at `>=20.0.0`. EBADENGINE warnings from `readdirp`/Astro during `npm install` are advisory only. Any future Astro WI in `product-website/` inherits this override; if it ever gets dropped, `npm run check` will RED with ERR_REQUIRE_ESM.

## Known gaps in the scaffold (referenced but missing)

- `.claude/hooks/` — the agents' `PreToolUse` Bash-allowlist hooks (`designer-bash-allowlist.sh`, `backend-developer-bash-allowlist.sh`, `frontend-developer-bash-allowlist.sh`, `reviewer-bash-allowlist.sh`) and the conductor's `reinject-state.sh` / `split-compound-commands.sh` do not exist; agent Bash hooks will fail until created or removed from the agent frontmatter.
- `.claude/schemas/` — `pipeline-state.v1.json`, `spec.v1.json`, and the work-items schema referenced by the conductor and `rules/schema-review.md` do not exist.
- Agent frontmatter names MCP servers (`cwm-roslyn-navigator`, `serena`, `context7`, `microsoft-docs`) and skills (`architecture-advisor`, `vertical-slice`, `security-scan`, `superpowers:*`) not configured in this repo.
- `.claude/assignement/` is empty (note the folder-name typo); the actual assignments live in `.claude/state/`.
- `.claude/skills/taxi-order-state-machine/taxi-order-state-machine.skill` is a zip archive, not an unpacked `SKILL.md` — it may not load as a skill until extracted.
