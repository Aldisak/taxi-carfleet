{
  "wi": "WI-1",
  "wi_id": "WI-1",
  "uc": "UC-016",
  "status": "complete",
  "files_changed": [
    "web/src/features/customer/tracking/headlineRules.ts",
    "web/src/features/customer/tracking/headlineRules.test.ts",
    "web/src/features/customer/tracking/markerInterpolation.ts",
    "web/src/features/customer/tracking/markerInterpolation.test.ts",
    "web/src/shared/i18n/cs-CZ.json",
    "web/src/shared/i18n/en-US.json",
    "web/src/shared/i18n/ru-RU.json",
    "web/src/shared/i18n/uk-UA.json",
    "web/src/shared/i18n/fil-PH.json",
    "web/src/shared/i18n/de-DE.json"
  ],
  "tests_added": [
    "headlineRules.test.ts: rewrote per-status assertions for the new `phase` discriminant and the Assigned->searching heading change (13 tests)",
    "markerInterpolation.test.ts: 10 tests (midpoint lerp, elapsed==duration->next, elapsed>duration->next, elapsed==0->prev, negative elapsed->prev, null prev->next snap, duration==0->next no div-by-zero, fake-timer tick sequence converging to target, shouldAnimate reduced-motion true/false)"
  ],
  "tests_passing": true,
  "coverage_delta": "n/a (scoped run reports pass/fail only)",
  "red_observed": true,
  "red_details": [
    "headlineRules: RED observed - 11/13 failed, `h.phase` was undefined before adding the discriminant.",
    "markerInterpolation: RED observed - import of the non-existent module failed transform (no tests collected).",
    "i18n parity: RED observed - after adding the 2 keys to cs-CZ only, locales.parity.test.ts failed on 5 locales (extra: customer.tracking.reorder, customer.tracking.sheetLabel). Green after adding to all 6."
  ],
  "verification": { "tool": "vitest", "filter": "features/customer/tracking", "exit_code": 0, "result": "14 files, 84 tests passing" },
  "verification_output": "Test Files 14 passed (14); Tests 84 passed (84); EXIT: 0",
  "tsc": "pass (tsc --noEmit clean)",
  "lint": "pass (eslint --max-warnings 0 + check:geo, 0 warnings)",
  "hit_max_turns": false,
  "notes": "Deliverable 1 (headlineRules): added `TrackPhase` union + `phase` field on HeadlineDescriptor. Split the shared Assigned/Accepted case: backend status New AND Assigned collapse to phase 'searching' with key 'customer.tracking.headline.new' (behavior change per spec section 2 - Assigned no longer emits assignedNoEta). Backend status Accepted -> phase 'assigned' (keeps assignedEta/assignedNoEta). Deliberate name inversion (status Assigned->phase searching; status Accepted->phase assigned) documented in a doc comment. Assigned keeps showCancel true. Unknown -> safe 'searching', cancel blocked. Existing flags + TrackVm unchanged. Existing TrackingPage.test.tsx consumer stays green (does not assert the Assigned heading string directly).\n\nDeliverable 2 (markerInterpolation): new PURE module. interpolateCoord guards duration<=0 || elapsed>=duration BEFORE division (no div-by-zero), elapsed<=0->prev, null prev->next snap. shouldAnimate(prefersReducedMotion) is the pure reduced-motion decision (no matchMedia - DOM read is WI-3's). Imports only LatLng type from trackingMarker.ts; nothing from leaflet/react/DOM. Per-frame rAF tick intentionally NOT here (WI-3).\n\nDeliverable 3 (i18n): DESIGN-REVIEW FINDING F3 IS FACTUALLY WRONG - customer.tracking.reorder did NOT exist; only customer.history.reorder does. Grepped all 6 locales to confirm. Added TWO genuinely-new keys under customer.tracking in ALL SIX locales, formal 'vy': reorder (cs 'Objednat znovu', en 'Order again', ru/uk/fil/de translated) and sheetLabel (cs 'Stav jizdy', en 'Ride status', etc.) for BottomSheet's required ariaLabelKey. Chose a dedicated tracking-namespace reorder over coupling WI-2 to customer.history.reorder (WI-2 files_touched excludes locale JSONs, so all WI-2 strings must land here). All other WI-2 strings already exist and are reused as-is. German 'Fahrtstatus' has no umlaut; no diacritic-scan issue. Money cs-CZ/Kc + dates Europe/Prague untouched. parity/formatStaysCs/extensibility/locales.sentinel all green.\n\nScope: only WI-1 files_touched staged. Did not touch WI-2/3/4 files. NOTE: this handoff filename previously held a stale UC-015 WI-1 result (documented filename-collision trap); overwritten with UC-016 WI-1."
}
