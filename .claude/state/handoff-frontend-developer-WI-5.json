{
  "wi_id": "WI-5",
  "uc": "UC-021",
  "lane": "web",
  "files_changed": [
    "web/src/features/orders/OrderDrawer.tsx",
    "web/src/features/orders/OrderDrawer.test.tsx",
    "web/src/features/orders/OrderEventTimeline.tsx",
    "web/src/features/orders/OrderEventTimeline.test.tsx",
    "web/src/features/orders/NotificationsSection.tsx",
    "web/src/features/orders/transitionButtons.ts",
    "web/src/features/orders/transitionButtons.test.ts",
    "web/src/features/orders/orderStatusTone.ts",
    "web/src/features/orders/orderStatusTone.test.ts"
  ],
  "files_touched": [
    "web/src/features/orders/OrderDrawer.tsx",
    "web/src/features/orders/OrderDrawer.test.tsx",
    "web/src/features/orders/OrderEventTimeline.tsx",
    "web/src/features/orders/OrderEventTimeline.test.tsx",
    "web/src/features/orders/NotificationsSection.tsx",
    "web/src/features/orders/transitionButtons.ts",
    "web/src/features/orders/transitionButtons.test.ts",
    "web/src/features/orders/orderStatusTone.ts",
    "web/src/features/orders/orderStatusTone.test.ts"
  ],
  "tests_added": 21,
  "tests_passing": 52,
  "coverage_delta": "not measured (WI did not request a coverage run; scoped tests all green)",
  "transitionButtons_change": {
    "signature_before": "deriveTransitionButtons(allowedActions: string[]): { action: string; label: string; needsDriverPicker: boolean; needsReason: boolean }[]",
    "signature_after": "deriveTransitionButtons(allowedActions: string[]): { action: string; needsDriverPicker: boolean; needsReason: boolean }[]",
    "change": "Dropped the hardcoded Czech `label` field and the ACTION_LABELS map. Labels now come from i18n at the consumer via t('orders.actions.' + action).",
    "consumer_verification": "grep '\\.label\\b' across web/src/features found exactly one non-test read at OrderDrawer.tsx (old line ~735); it now renders t('orders.actions.' + btn.action, btn.action). No other module reads .label off the result. transitionButtons.test.ts asserts buttons[0] does NOT have a 'label' property (RED observed against the label-returning impl, green after removal)."
  },
  "new_stat_cards": {
    "customer": {
      "label_key": "orders.drawer.customer",
      "fields": ["customerPhone (orders.drawer.fields.phone)", "customerName (orders.drawer.fields.name, hidden when null)", "passengers rendered 'Cestující: N' (orders.drawer.fields.passengers)"]
    },
    "price": {
      "label_key": "orders.drawer.priceCard",
      "fields": ["amount = fixedPriceCzk when priceType==='Fixed' else estimatedPriceCzk, formatted Intl.NumberFormat('cs-CZ') + ' Kč', '—' when null (orders.drawer.fields.amount)", "priceType label orders.drawer.priceFixed / priceEstimated (orders.drawer.fields.priceType)", "matchedRoute = 'pickup → dropoff' route text — DTO has no matchedRoute field (orders.drawer.fields.matchedRoute)"]
    },
    "component": "Both use desk-kit Stat with a composed ReactNode value (StatLines) so multiple lines fit the KPI slot."
  },
  "tabs_wiring": {
    "component": "shared desk Tabs (role=tablist, role=tab, arrow-key nav, active underline)",
    "panels": "role='tabpanel', id='panel-history'/'panel-notifications', aria-labelledby='tab-history'/'tab-notifications' (desk Tabs generates tab ids 'tab-<id>'); only the active panel is mounted",
    "history_panel": "EventTimeline restyled onto desk Timeline (newest-first, accent dot on latest, caption folds 'Actor · relative time · reason')",
    "notifications_panel": "NotificationsSection restyled: channel DeskPill (SMS/Push) + status DeskPill (Odesláno/Selhalo/Ve frontě/Přeskočeno), status wrapped in role='status' for AT",
    "aria_controls_gap": "shared desk Tabs emits role='tab'+id but NOT aria-controls (out-of-scope to edit); panels carry aria-labelledby back to their tab as the achievable half"
  },
  "i18n_requests": [
    "orders.source.Dispatcher / orders.source.Phone / orders.source.App (and any other OrderSource enum values): needed to localize the order source in the drawer created line. The DTO exposes `source` as a raw enum string; showing it verbatim would violate #i18n-czech-first, so `source` was DROPPED from the created line (now 'Vytvořeno · <Prague time>' only). Add these keys to all 6 locales, then the created line can render t('orders.source.' + order.source)."
  ],
  "data_gaps": [
    "OrderDetailDto has no `actor` field — created line shows 'Vytvořeno · <Prague time>' only; actor omitted.",
    "OrderDetailDto exposes `source` only as a raw enum string with no i18n keys — dropped from the created line (see i18n_requests) to avoid an untranslated user-facing string.",
    "OrderDetailDto has no matchedRoute/matchedRouteId — Cena card matchedRoute row shows pickup → dropoff route text as the closest available data."
  ],
  "test_counts": {
    "transitionButtons": 16,
    "orderStatusTone": 6,
    "OrderEventTimeline": 6,
    "NotificationsSection": 5,
    "OrderDrawer": 19
  },
  "verification_output": {
    "vitest": "npx vitest run src/features/orders/OrderDrawer src/features/orders/OrderEventTimeline src/features/orders/NotificationsSection src/features/orders/transitionButtons -> 4 files / 46 tests passed, exit 0. Full scoped run incl. orderStatusTone = 5 files / 52 tests passed.",
    "tsc": "npx tsc --noEmit -> exit 0, 0 errors on the final run. (Earlier an AdminFleetsPage TS2322 '<DeskButton as={Link}>' error flickered from a concurrent lane mid-editing that out-of-scope, unstaged file; WI-5's scoped files compile clean and the final tsc run was 0 errors once that lane settled.)",
    "eslint": "npx eslint src/features/orders --max-warnings 0 -> exit 0, clean."
  },
  "deviations": [
    "Restyle-in-place (NOT desk Drawer component): kept routing-driven open/close, Overlay onClick-to-close (existing test relies on it), and the exact a11y contract (role='dialog' + aria-modal + aria-label = orders.drawer.title while header shows 'Objednávka #code'). Desk Drawer would couple title -> both h2 and aria-label and break the click-scrim-close test. WI blesses this fallback.",
    "getOrderStatusTone colocated as new pure module features/orders/orderStatusTone.ts (with test) instead of importing features/board/statusPill.ts (banned cross-feature import) or promoting to shared/ (out of file scope). Intentional small duplication documented in the module JSDoc.",
    "aria-controls not set on tabs (shared desk Tabs out of scope); panels carry aria-labelledby.",
    "Desk Timeline caption is one line, so 'Actor · relative time' and reason are folded into a single caption string.",
    "Created line omits actor and source; matchedRoute shows route text. actor/matchedRoute are DTO data gaps; source is a data+i18n gap (raw enum, no keys) reported in i18n_requests."
  ],
  "hit_max_turns": false,
  "notes": "TDD red-green-refactor per cycle: transitionButtons label-drop (RED observed), orderStatusTone new module (RED observed), OrderEventTimeline behavior tests, NotificationsSection restyle (kept green incl. axe), OrderDrawer 8 new tests (RED observed: 7 failing incl. empty-label axe button-name violation, then green). No memoization added (no measured re-render problem). Money Intl.NumberFormat('cs-CZ') + ' Kč'; dates Europe/Prague. Kept useUpdateOrder/useAssignOrder/useReassignOrder/useCancelOrder/eventTimeline/isOrderEditable/cancelReasons/useAddressSuggest logic untouched. No locale JSONs edited. Only web/src/features/orders/* staged; SearchPage/SearchFilters and board files untouched. POST-REVIEW FIXES: (a) restored a read-mode 'Kdy'/scheduledAt line — the rewrite had accidentally gated it behind isEditing, hiding pickup time in read mode; added 2 tests (read-mode time shows, ASAP fallback). (b) removed the raw `source` enum from the created line (untranslated string vs #i18n-czech-first) and filed the orders.source.* i18n_request. NOTE: this file previously held a stale UC-019 WI-5 handoff (handoff filenames are keyed on WI-<n> only and collide across UCs) — overwritten with this UC-021 result."
}
