{
  "wi": "WI-5",
  "wi_id": "WI-5",
  "uc": "UC-019",
  "status": "complete",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/features/driver/map/DriverMapScreen.tsx",
    "web/src/features/driver/map/DriverMapScreen.test.tsx",
    "web/src/app/DriverLayout.tsx",
    "web/src/app/DriverLayout.test.tsx",
    "web/src/app/router.tsx",
    ".claude/state/handoff-frontend-developer-WI-5.json"
  ],
  "files_removed": [
    "web/src/features/driver/home/DriverHomePage.tsx",
    "web/src/features/driver/ride/DriverRidePage.tsx",
    "web/src/features/driver/ride/DriverRidePage.test.tsx",
    "web/src/features/driver/offer/OfferTakeover.tsx",
    "web/src/features/driver/ride/RideMapStrip.tsx",
    "web/src/features/driver/ride/RideMapStrip.test.tsx",
    "web/src/features/driver/ride/RideMapInner.tsx"
  ],
  "tests_added": 17,
  "tests_passing": 20,
  "coverage_delta": "+DriverMapScreen.tsx (new assembly screen, 13 behavior tests incl. F-2 mount pins, complete-nav, stale-banner, atomic-selector no-loop, 3 axe states) + 4 new DriverLayout tests (OfferCard-swap, position/queue mounts, BottomNav visible@order==null / hidden@order!=null). Removed 2 test files (DriverRidePage.test, RideMapStrip.test) whose behavior is superseded by RideSheet.test (WI-4) + DriverMapScreen.test.",
  "red_observed": true,
  "red_detail": "Cycle 1 (DriverMapScreen): RED = 'Failed to resolve import ./DriverMapScreen' (module missing) -> implemented -> 13 green. Cycle 2 (DriverLayout WI-5 tests): RED = 2 failing (offer-card testid absent because DriverLayout still rendered OfferTakeover; BottomNav still present when activeOrder set) -> swapped OfferTakeover->OfferCard + gated BottomNav on activeOrder==null -> 7 green.",
  "tsc": "npm run --prefix web tsc -> 0 errors (PRIMARY no-dangle gate: proves none of the 7 removals left a dangling import). Two transient test-only errors fixed during the cycle (unused ReactNode import; vi.fn generic single-arg form for the arrive/start/cancelNoShow Outcome mocks).",
  "lint": "npm run --prefix web lint -> pass (check:geo OK + eslint --max-warnings 0, 0 warnings).",
  "verification": "npm-build -> exit 0 (fresh re-run). tsc+build succeed (no dangle), then npm run --prefix web size -> all three budgets green.",
  "verification_output": "npm run build -> vite built in 5.58s + PWA precache 32 entries, BUILD_EXIT=0. npm run size -> app js (eager+lazy non-analytics/non-admin): 195.95 kB / 260 kB limit; analytics chunk: 94.87 kB / 115 kB; admin chunk: 64 kB / 70 kB — all within budget. Full driver+app vitest: 56 files / 348 tests passed.",
  "leaflet_lazy": "CONFIRMED. Grep 'from react-leaflet' under web/src/features/driver -> ONLY DriverMapInner.tsx. DriverMapScreen imports DriverMapInner via lazy(() => import('./DriverMapInner')) inside <Suspense fallback={null}> — the sole import site. dist shows a distinct DriverMapInner-DbaJAMXU.js lazy chunk that carries leaflet-container; its existence proves the split held (Rollup would have inlined leaflet into index had any eager-reachable driver module statically imported it). DriverMapScreen itself lands in the eager index chunk (correct). The leaflet-container in index-*.js is PRE-EXISTING from the dispatcher BoardPage's static react-leaflet import (router.tsx line 6) — documented verbatim in CLAUDE.md UC-010 WI-15; not a WI-5 regression. The index reference to the DriverMapInner-*.js filename is the dynamic-import runtime manifest, the opposite of a static import.",
  "removal_order": "ATOMIC, ordered to keep tsc always green: (1) wrote DriverMapScreen + test; (2) swapped DriverLayout OfferTakeover->OfferCard + BottomNav conditional; (3) re-pointed router index to DriverMapScreen + deleted the 'ride' child + DriverHomePage/DriverRidePage imports; (4) THEN git rm'd the 7 modules. App never referenced a deleted module at any intermediate step. Grep-confirmed before deletion: RideMapInner's only importer was RideMapStrip (also removed); all removed-module importers were router.tsx/DriverLayout.tsx (edited) or removed files. Remaining textual hits post-removal are comments/test-descriptions only — zero dangling imports (tsc 0 errors is authoritative).",
  "notes": "ADVISOR-CRITICAL ORDER-GATING (trap A): DriverMapScreen renders RideSheet on `order != null` (from useActiveOrder), NOT on useDriverMe status; DriverLayout hides BottomNav on `useActiveOrderStore(s=>s.order) == null` (atomic selector) — SAME signal, so no accept/hub race where status flips before order hydrates. Status picks Offline-vs-Free ONLY when order==null; a transient (status Busy/EnRoute but order not yet restored) renders map + minimal panel, never RideSheet (needs a non-null order). F-2 RE-HOME (design-review MEDIUM folded in): all re-homed session hooks are called UNCONDITIONALLY at the top of DriverMapScreen (never inside a branch) — useRideRestore(myDriverId), useLiveClear(myDriverId), useOwnStatusSync(myDriverId), useActiveOrder(), useWakeLock(order != null), useQueuePendingCount(), useRideTransition(), useRouteGeometry({leg,own,order}). useRideRestore runs in EVERY state (correct — cold-load restore, AC#6 server-over-IDB authority via reconcileActiveRide). The F-2 mount test vi.mocks useRideRestore/useLiveClear/useWakeLock and asserts each is called on mount in the EnRoute+order state (useRideRestore/useLiveClear with 'd1'; useWakeLock(true)) + a second test asserts useWakeLock(false) when order==null — pins the re-home so a future silent drop goes RED (tsc cannot catch an omitted hook call). runTransition wrapper owns outcome.type==='stale' -> setReconciledNote(true) reassigned-banner logic, passed to RideSheet. PORTED DriverHomePage effects (AC explicit): ensureSubscribed() push effect on mount + Permissions API geolocation check (hasLocation -> disables 'Začít směnu' + shows noLocationHint). useRouteToast rendered as a role=status Toast (where 'Hotovo ✓' surfaces post-complete + F-04 reassigned). LAYOUT (advisor + AC#1/#2): screen is position:relative flex:1 (fills DriverLayout <Content>, now position:relative overflow:hidden); MapLayer position:absolute inset:0 z-index:map (definite size, avoids jsdom height:100% collapse trap). Offline/Free panels are IN-SCREEN absolute BottomSlot overlays (NOT shared/ui/BottomSheet) so they never collide with the VISIBLE BottomNav (order==null in those states); only RideSheet (WI-4, fixed BottomSheet) uses BottomSheet, and only then does DriverLayout hide BottomNav. BottomSlot left-padded off the bottom-left Mapy logo (CLAUDE.md UC-014 WI-2). ATOMIC zustand selectors throughout (useOwnPositionStore(s=>s.position), useActiveOrderStore(s=>s.order)) — no object selectors (laneB3c loop trap); a no-loop smoke test asserts no console.error on render. No premature memoization (module-level NO_LEG_ORDER const for the null-order route-geometry skip). ROUTER: /driver index -> DriverMapScreen (CHILD of DriverLayout, not sibling — session mounts live in DriverLayout, Assumption 2); DELETED { path:'ride' } child + DriverHomePage/DriverRidePage imports; KEPT ride/complete, history, settings, /driver/login. Accept no longer navigates (WI-2); state-driven screen reactively renders the ride view. DID NOT touch OfferCard/DriverMapInner/RideSheet (consumed as-is per WI-2/3/4 prop contracts) or locale JSONs (all keys front-loaded in WI-1; verified driver.map.*, driver.home.status.waitingForOrder/startShift/endShift, driver.nav.*, driver.ride.etaTo* present in cs-CZ). Left StatusButton/ConnectionDot/homeState/useMySummary in place (now-unused-but-harmless per AC#7 — deleting a still-referenced one would dangle tsc). *** WI-6 HEADS-UP (non-blocking, MUST retarget): the map-first Free state intentionally has NO summary chips (old DriverHomePage chips removed by WI-5 AC design). WI-6's existing 'summary chips 1 ride + cash total' assertion in Driver_FullFlow is now INVALID — retarget it (completion-summary lives on /driver/ride/complete + the Free-state 'Hotovo' toast). DO NOT add chips back — contradicts the WI-5 AC. Also: offer is now role='region' name 'Nová objednávka' (not dialog 'Nástup'); after Accept the ride view appears ON /driver (no waitForURL('/driver/ride')). *** Scope: DriverMapScreen.tsx + .test.tsx (new), DriverLayout.tsx + .test.tsx (edited — extended the existing boot-test file with a new describe for offer/BottomNav), router.tsx (edited), 7 modules removed via git rm. Staged with explicit paths + git rm; nothing committed. e2e NOT run (WI-6 owns it)."
}
