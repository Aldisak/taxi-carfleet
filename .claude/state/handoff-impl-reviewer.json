{
  "$schema": ".claude/schemas/impl-review.v1.json",
  "kind": "impl",
  "uc": "UC-016",
  "wi": "WI-1",
  "round": 1,
  "wi_id": "WI-1",
  "commit_range": "staged",
  "blocks_merge": false,
  "findings": [],
  "summary": {
    "CRITICAL": 0,
    "HIGH": 0,
    "MEDIUM": 0,
    "LOW": 0
  },
  "automated_checks": {
    "eslint": "PASS - fresh npm run lint (check:geo + eslint --max-warnings 0) clean, 0 warnings, exit 0.",
    "tsc": "PASS - fresh npx tsc --noEmit clean, 0 errors, exit 0.",
    "vitest": "PASS fresh re-run scoped to WI-1: headlineRules.test.ts + markerInterpolation.test.ts + locales.parity.test.ts + formatStaysCs.test.ts = 30/30 green (exit 0). Broader tracking+parity run 89/89 green (15 files).",
    "scope": "EXACT: only the 10 WI-1 files_touched staged (headlineRules.ts/.test.ts, markerInterpolation.ts/.test.ts, 6 locale JSONs). No out-of-scope changes.",
    "purity_check": "markerInterpolation.ts imports ONLY `type { LatLng }` from ./trackingMarker (a plain interface, no runtime deps); nothing from leaflet/react/DOM. Eager-safe as required.",
    "f3_verification": "design-reviewer F3 (LOW, 'customer.tracking.reorder already exists at cs-CZ.json:1051, do not re-declare') is FACTUALLY WRONG and correctly rebutted by the developer. Verified against git HEAD: pre-change cs-CZ had customer.history.reorder='Objednat znovu' (the line-1051 match) but customer.tracking.reorder was absent (None) and customer.tracking.sheetLabel absent (None). Both new keys are genuinely new. Not flagged.",
    "headline_behavior": "Confirmed load-bearing change: backend status New AND Assigned both -> phase 'searching' + key 'customer.tracking.headline.new' (Assigned no longer emits assignedNoEta); Accepted -> phase 'assigned' keeping assignedEta/assignedNoEta; unknown -> safe 'searching' + cancel blocked. Existing flags/TrackVm/HeadlineDescriptor extended (phase added), not rewritten. Existing test updated in-place (not a stale-test break); RED was observed per developer handoff.",
    "i18n_parity": "All 6 locales receive the SAME two new keys (reorder, sheetLabel) as minimal 2-line insertions; formal 'vy' tone; no whole-file reformat; no re-declared existing keys; money cs-CZ/Kc and Europe/Prague dates untouched. German 'Fahrtstatus'/'Erneut bestellen' carry no umlaut so no diacritic-scan interaction (and per CLAUDE.md German ä/ö/ü/ß would be legitimate anyway).",
    "note": "The tree e2e (customer.spec.ts Assigned-heading) is intentionally left RED pending WI-4 per by-design sequencing -- NOT a WI-1 defect, not flagged. The act(...) warning in the broader tracking run originates from TrackingPage.test.tsx (a WI-4 file, untouched by WI-1) and is pre-existing noise."
  }
}