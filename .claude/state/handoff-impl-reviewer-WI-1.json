{
  "$schema": ".claude/schemas/impl-review.v1.json",
  "kind": "impl",
  "uc": "UC-018",
  "wi": "WI-1",
  "wi_id": "WI-1",
  "round": 1,
  "commit_range": "staged",
  "blocks_merge": false,
  "findings": [],
  "summary": { "CRITICAL": 0, "HIGH": 0, "MEDIUM": 0, "LOW": 0 },
  "automated_checks": {
    "build_warnaserror": "PASS - fresh C:\Users\alesm\AppData\Local\Microsoft\dotnet\dotnet.exe build api -warnaserror: 0 warnings, 0 errors, exit 0.",
    "scoped_tests": "PASS - fresh dotnet test api --filter FullyQualifiedName~Taxi.Api.Tests.Geo --no-build: 139 passing, 0 failing, 0 skipped, exit 0 (covers MapyClientHandlerTests + GeoServiceTests).",
    "scope": "EXACT: only the 5 WI-1 files_touched staged (IMapyClient.cs, MapyClient.cs, GeoService.cs, GeoServiceTests.cs, MapyClientHandlerTests.cs). No out-of-scope changes.",
    "coord_order": "CORRECT. IMapyClient.SuggestAsync takes (double Lat, double Lng)? near; GeoService forwards the tuple unchanged on both paths; MapyClient flips ONCE to preferNear={lng},{lat}. Test SuggestAsync_NearSupplied_RequestUrlContainsPreferNear pins the literal preferNear=14.43,50.08 for near=(50.08,14.43) (lng-first) plus a NotContain(preferNear=50.08) guard against a lat-first regression. A silent swap cannot pass.",
    "invariant_culture": "CORRECT. Both coords formatted via CultureInfo.InvariantCulture (System.Globalization imported); test asserts dot-decimal 14.43 / 50.08 and documents that cs-CZ 14,43/50,08 would be caught.",
    "both_paths_forwarding": "CORRECT. near forwarded on the Guid.Empty fleetless bypass (GeoService.cs:31) AND the cache-miss factory closure (GeoService.cs:40); each pinned by FakeMapyClient.LastSuggestNear == near (Suggest_FleetlessBypass_ForwardsNearToClient, Suggest_CachedFactoryPath_ForwardsNearToClient).",
    "present_absent": "CORRECT. preferNear present-when-supplied / absent-when-null pinned by SuggestAsync_NearSupplied_RequestUrlContainsPreferNear and SuggestAsync_NearNull_RequestUrlOmitsPreferNear.",
    "signature_completeness": "CORRECT. Interface IMapyClient.cs itself edited (not just impl) with XML <param name=near> doc; all 5 MapyClientHandlerTests.SuggestAsync call sites updated to near:null; FakeMapyClient gains LastSuggestNear; CountingMapyClient signature-only. grep confirms MapyClient is the only production implementer (IGeoService.cs + GeoFeatureConfiguration.cs matches are comment/DI-only); FakeGeoService correctly NOT touched.",
    "unchanged_surfaces": "CONFIRMED. No route/verb/policy/result-shape change; rate limiter, key-missing short-circuit (MissingKey), and GeoResult.Unavailable degradation untouched; cache key unchanged (GeoCacheKey.Suggest already includes near).",
    "csharp_style": "CLEAN. XML doc on the changed interface member; nullable tuple correct; internal sealed preserved; InvariantCulture per rules/csharp-style. No violations of the WI rule_citations.",
    "note": "Zero findings; blocks_merge=false. .claude/schemas/impl-review.v1.json does not exist (documented scaffold gap in CLAUDE.md); handoff shape mirrors the existing .claude/state/handoff-impl-reviewer.json precedent."
  }
}
