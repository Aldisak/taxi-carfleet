{
  "wi": "WI-2",
  "uc": "UC-015",
  "status": "complete",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/features/customer/order/DestinationSearch.tsx",
    "web/src/features/customer/order/DestinationSearch.test.tsx",
    ".claude/state/handoff-frontend-developer-WI-2.json"
  ],
  "tests_added": 8,
  "red_observed": true,
  "full_suite": "pass (235 files, 1621 tests)",
  "tsc": "pass",
  "lint": "pass (0 warnings; check:geo OK)",
  "build": "pass (vite build + PWA injectManifest)",
  "size": "pass (app 197.32/260 kB, analytics 94.79/115 kB, admin 63.97/70 kB brotli)",
  "notes": "DestinationSearch.tsx is the top 'Kam to bude?' overlay for the shell topSlot. Presentational only: onSelectDestination(place: SelectedPlace) callback; the page (WI-4) owns orderFlowState (component does NOT call selectDestination — imports SelectedPlace as a type only, per the handoff AC 'component only calls back'). Reuses useSuggest directly (anonymous-by-slug, works logged-out — AC#1, verified by a no-token test) and suggestionMeta for the label+municipality secondary line (AC#2, rendered as plain text so it is part of the option's accessible name). Pending state uses shared SearchingLoader (role=status, default customer.loader.searching key). ARIA combobox: keyboard lives on the INPUT (ArrowUp/Down move active option, Enter selects, Escape collapses); active option tracked via aria-activedescendant; focus never leaves the input (deliberate divergence from AddressAutocomplete's per-<li> tabIndex/onKeyDown). IDREF HYGIENE (advisor #2): aria-controls and aria-activedescendant are emitted ONLY while the list is open, so no dangling IDREF reaches the DOM when collapsed — guarded by a dedicated collapsed-state axe assertion in addition to the open-state one (both green). No new i18n keys: customer.mapOrder.searchPlaceholder does double duty (placeholder + aria-label on the input), searchEmpty for no-results, SearchingLoader's default for pending — parity stays trivially green (no locale files touched). Leaflet stays lazy (DestinationSearch imports no leaflet). Error path: empty-list (200-empty, or non-200 kept-last via useSuggest) shows the no-results message, never a raw code. Tests use real timers + findBy* (300ms useSuggest debounce resolves under polling); pending test mocks getGeoSuggest with a never-resolving promise. Scoped filter 'DestinationSearch' green (8/8). Only WI-2's two files + this handoff staged; WI-1 staged files and pre-existing/queued files untouched. NOTE: overwrote a stale UC-014-era handoff-frontend-developer-WI-2.json (WI numbers repeat across UCs — documented collision in CLAUDE.md); UC-014 WI-2 detail recoverable from git history if needed."
}
