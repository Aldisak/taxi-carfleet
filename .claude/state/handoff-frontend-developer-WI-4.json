{
  "wi": "WI-4",
  "uc": "UC-019",
  "status": "complete",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/features/driver/ride/RideSheet.tsx",
    "web/src/features/driver/ride/RideSheet.test.tsx",
    ".claude/state/handoff-frontend-developer-WI-4.json"
  ],
  "tests_added": 8,
  "tests_passing": 8,
  "coverage_delta": "+RideSheet.tsx (new presentational component, 8 behavior tests incl. axe)",
  "red_observed": true,
  "tsc": "no RideSheet errors (grep 'ridesheet' on tsc output => 0 hits). Full `npm run --prefix web tsc` currently reports 3 errors from CONCURRENT sibling WIs only: OfferCard.test.tsx (WI-2, unused afterEach + missing ./OfferCard) and DriverMapInner.test.tsx (WI-3, missing ./DriverMapInner). None touch RideSheet; out of this WI's scope.",
  "lint": "pass (0 warnings) on RideSheet.tsx + RideSheet.test.tsx via `npx eslint --max-warnings 0`",
  "verification": "vitest filter features/driver/ride/RideSheet -> exit 0 (1 file, 8 tests, fresh re-run)",
  "verification_output": "vitest run features/driver/ride/RideSheet -> Test Files 1 passed (1), Tests 8 passed (8), EXIT 0",
  "notes": "PRESENTATIONAL PROP CONTRACT (RideSheetProps, exactly per WI-4 AC#1 - NO open/expanded/onClose props; RideSheet owns open/expanded locally like TrackingSheet): { order: OrderDetailDto, noShowEnabled: boolean, noShowCountdownSeconds: number|null, etaMinutes: number|null, onArrive: () => void, onStart: () => void, onComplete: () => void, onNoShow: () => void, isPending: boolean, pendingCount: number, reconciledNote: boolean }. All four handlers are bare () => void - WI-5's runTransition wrapper binds order.id and owns useRideTransition/useActiveOrder/useQueuePendingCount + the 'stale'->reconciledNote logic; RideSheet just invokes onXxx on click (tests use plain vi.fn() spies). RideSheet calls NO transition/data hooks and imports NO api client (only the OrderDetailDto TYPE). BUTTON WIRING lifted verbatim from DriverRidePage: deriveRideButtons(order.status, noShowEnabled, noShowCountdownSeconds, hasDropoff) -> RideButton; handlePrimary routes arrive/start/complete by primaryAction; handleSecondary calls onNoShow for the noShow secondary. NAVIGATE HANDOFF (advisor-flagged duplicate-nav trap AVOIDED): NavHandoff is rendered as the real <a> gated on buttons.navAction==='navigate' && buttons.navLabel, AND RideButton receives {...buttons, navAction:null, navLabel:null} so its internal nav <button> is suppressed - exactly the DriverRidePage pattern; test asserts the anchor href directly (contains pickupLat 49.95 for Accepted, dropoffLat 49.96 for InProgress) rather than mocking buildNavUrl. Nav target computed via navToDropoff = status==='InProgress' && hasDropoff -> dropoff coords else pickup coords, labelKey from buttons.navLabel (driver.ride.navigate / driver.ride.navigateToDropoff). NOTE per rideButtonState (reused unchanged): 'Arrived' has navAction:null so NO Navigate button on Arrived - the AC prose 'pickup while Accepted/Arrived' is superseded by the derived state; matches DriverRidePage and test 2 (asserts only primary+no-show). ETA line keys off status: driver.ride.etaToDropoff for InProgress, driver.ride.etaToPickup otherwise; {{min}} interpolation param is 'min'; whole line omitted when etaMinutes==null (test 6). BOTTOMSHEET: wraps shared/ui/BottomSheet (ariaLabelKey='driver.ride.sheetLabel', open always true while mounted since parent only mounts RideSheet during an active ride, expanded via local useState). onClose is wired to setExpanded(false) - Escape COLLAPSES, never unmounts (ride controls persist); BottomSheet supplies role=dialog + i18n aria-label + focus-in-on-open + Escape + focus-restore + non-modal (Mapy logo reachable). Test 8 asserts the dialog persists after Escape (observable) + axe clean via the shared ../../../shared/test/axe helper (color-contrast disabled for jsdom). Touch targets inherited: RideButton PrimaryButton=theme.touchTargets.primary, NavHandoff/SecondaryButton=theme.touchTargets.min; PendingBadge (role=status) shown when pendingCount>0; reassigned banner role=alert t('driver.ride.reassigned') when reconciledNote. i18n: uses ONLY WI-1 keys (all present in cs-CZ: sheetLabel/etaToPickup/etaToDropoff/reassigned + existing navigate/navigateToDropoff/arrive/start/complete/noShow/noShowCountdown/pickup/dropoff/phone/note + queue.pending); ZERO locale JSON edits, no new keys. Money not rendered in RideSheet (price shown on complete page). Scope: ONLY RideSheet.tsx + RideSheet.test.tsx created; DriverRidePage/OfferCard/DriverMapInner/DriverMapScreen/router/DriverLayout/RideButton/NavHandoff/BottomSheet/locale JSONs untouched (WI-5 wires the hooks + removes DriverRidePage). Staged with explicit paths (git add web/src/features/driver/ride/RideSheet.tsx + .test.tsx); nothing committed. NOTE: overwrote a stale UC-016-era handoff-frontend-developer-WI-4.json (WI numbers repeat across UCs - documented collision in CLAUDE.md); UC-016 WI-4 detail recoverable from git history."
}
