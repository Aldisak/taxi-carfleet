{
  "wi": "WI-4",
  "uc": "UC-015",
  "status": "in-progress",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/features/customer/order/MapOrderPage.tsx",
    "web/src/features/customer/order/MapOrderPage.test.tsx",
    "web/src/app/router.tsx",
    "web/src/app/customerRoutes.test.ts",
    "web/src/features/customer/history/HistoryRow.tsx",
    "web/src/features/customer/history/CustomerHistoryPage.test.tsx",
    "web/e2e/customer.spec.ts"
  ],
  "files_removed": [
    "web/src/features/customer/home/CustomerHomePage.tsx",
    "web/src/features/customer/home/CustomerHomePage.test.tsx",
    "web/src/features/customer/home/RouteCard.tsx",
    "web/src/features/customer/home/RouteCard.test.tsx",
    "web/src/features/customer/home/ActiveOrderBanner.tsx",
    "web/src/features/customer/home/ActiveOrderBanner.test.tsx",
    "web/src/features/customer/home/useCommonRoutes.ts",
    "web/src/features/customer/home/homeContent.ts",
    "web/src/features/customer/home/homeContent.test.ts",
    "web/src/features/customer/order/CustomOrderPage.tsx",
    "web/src/features/customer/order/CustomOrderPage.test.tsx",
    "web/src/features/customer/order/RouteOrderPage.tsx",
    "web/src/features/customer/order/RouteOrderPage.test.tsx",
    "web/src/features/customer/order/useRouteOrder.ts",
    "web/src/features/customer/order/useRouteOrder.test.tsx",
    "web/src/features/customer/order/AddressAutocomplete.tsx",
    "web/src/features/customer/order/AddressAutocomplete.test.tsx",
    "web/src/features/customer/order/PickupMap.tsx",
    "web/src/features/customer/order/PickupMap.test.tsx",
    "web/src/features/customer/order/PickupMapInner.tsx",
    "web/src/features/customer/order/FixedPriceBadge.tsx"
  ],
  "review_round": 2,
  "tests_added": [
    "web/src/features/customer/order/MapOrderPage.test.tsx (8 tests: search-in-top-slot, price-sheet-on-select, camera setView/fitBounds, onOrdered-navigate, onCancel, pickup-freeze, axe)",
    "web/src/app/customerRoutes.test.ts (4 tests: dual /customer route ranking guard; bare-/customer test hardened round-2 to assert the winning leaf is childless)"
  ],
  "red_observed": true,
  "full_suite": "NOT RE-RUN round-2 (round-1 reported 229 files, 1605 tests, 0 failed; conductor runs e2e separately)",
  "scoped_vitest_round2": "PASS: MapOrderPage.test.tsx 8/8 + customerRoutes.test.ts 4/4 (12/12 green)",
  "tsc": "PASS (0 errors)",
  "lint": "PASS (0 warnings; --max-warnings 0; check:geo OK)",
  "build": "PASS (vite build + sw.js)",
  "size": "PASS (app 194.61/260 kB, analytics 94.85/115 kB, admin 63.91/70 kB brotli)",
  "e2e": "PENDING",
  "round2_findings_resolved": [
    "HIGH memoization-policy (MapOrderPage.tsx): removed all four useCallback wrappers (handleCenterChange, handleSelectDestination, handleCancel, handleOrdered) — inlined as plain const fns; dropped the useCallback import. No measured re-render problem existed and none was effective (handleCenterChange goes through MoveendController's ref-to-latest; the sheet/search/shell children are not React.memo'd). Did NOT add React.memo to justify them. MapOrderPage tests stay green (8/8), behaviour identical.",
    "MEDIUM router-ranking guard (customerRoutes.test.ts): the bare-/customer test now asserts the last matched route's children === undefined, discriminating the childless MapOrderPage leaf from the CustomerLayout branch (which has a children array in router.tsx L153-156). Verified structurally: MapOrderPage route (L149-152) has no children key; if CustomerLayout won, winner.children would be a defined array and the assertion would fail. length===1 kept as the first-line check."
  ],
  "notes": "ROUND-2 FIX (both blocking findings resolved; scope limited to MapOrderPage.tsx + customerRoutes.test.ts per review; PriceSheet #ffffff LOW is WI-3 accepted precedent, untouched; locale files untouched). ORIGINAL: MapOrderPage assembled: owns orderFlowState, composes CustomerMapShell (topSlot=DestinationSearch, bottomSlot=PriceSheet shown only once destination set), drives usePriceQuote({allowAnonymous:true}), pickup from map center via debounced useReverseGeocode (search phase only - FROZEN once destinationSet so fitBounds center never overwrites pickup), cameraTarget from orderCamera.orderCameraPoints, onOrdered->navigate('/customer/t/'+code). Router: MapOrderPage is a SIBLING leaf at /customer (mirrors /customer/login), lazily; CustomerLayout branch (no index) keeps t/:code + history. Ranking verified via matchRoutes in customerRoutes.test.ts (bare /customer -> 1 leaf; /customer/history + /customer/t/:code -> 2 nested). Removed 21 old files. tsc (WI primary gate) clean = no dangling imports. HistoryRow 'Objednat znovu' retargeted /customer/order/new -> /customer (route deleted by this WI); reorder draft still passed in router state for forward-compat but the destination-first flow does not prefill from it (draft carried addresses without coords). e2e AC#1 rewritten to the map-first path (Customer_MapFirstCreateOrder_ToTracking): the keyless e2e harness (e2e-api.mjs --no-launch-profile does NOT inject Mapy__ServerKey) means /geo/suggest + /pricing/quote + /geo/reverse are stubbed via page.route (mirrors AC#4 tile-fulfill), but the REAL POST /orders + request-code/verify-code (with injected SMS hash) run end-to-end. Stubbed quote is an Estimate (routeId=null) not Fixed, so no fabricated route_id FK on the real create. No new i18n keys needed (all customer.mapOrder.* keys already existed from WI-2/WI-3). Leaflet stays out of eager path (MapOrderPage chunk does not statically import leaflet; CustomerMapBackground is the lazy importer)."
}
