{
  "wi": "WI-4",
  "uc": "UC-014",
  "status": "complete",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/shared/api/client.ts",
    "web/src/shared/api/getGeoReverse.test.ts",
    "web/src/features/customer/shell/useReverseGeocode.ts",
    "web/src/features/customer/shell/useReverseGeocode.test.ts",
    "web/src/features/customer/shell/centerPin.ts",
    "web/src/features/customer/shell/centerPin.test.ts",
    "web/src/features/customer/order/useSuggest.ts",
    "web/src/features/customer/order/useSuggest.test.ts",
    "web/src/shared/i18n/cs-CZ.json",
    "web/src/shared/i18n/en-US.json",
    "web/src/shared/i18n/ru-RU.json",
    "web/src/shared/i18n/uk-UA.json",
    "web/src/shared/i18n/fil-PH.json",
    "web/src/shared/i18n/de-DE.json"
  ],
  "tests_added": [
    "web/src/shared/api/getGeoReverse.test.ts (4: GET /geo/reverse?lat=..&lng=.. URL shape, 4-decimal rounding, dot-decimal invariant formatting, response mapping to {found,label,street,municipality})",
    "web/src/features/customer/shell/useReverseGeocode.test.ts (3: debounce+round+one-call with ['geo','reverse',lat,lng] key, disabled-without-coords, no cache thrash on sub-4th-decimal jitter)",
    "web/src/features/customer/shell/centerPin.test.ts (8: resolveCenterSource permission branches granted/denied/unavailable/prompt/granted-no-fix; createMoveendDebouncer settle/re-emit/cancel)",
    "web/src/features/customer/order/useSuggest.test.ts (INVERTED 'queries logged-out at >=3 chars' + added '<3 chars logged-out still no query' regression)"
  ],
  "red_observed": true,
  "full_suite": "231 test files, 1587 tests, all passing",
  "tsc": "pass (tsc --noEmit clean, strict, no any)",
  "lint": "pass (check:geo OK + eslint --max-warnings 0 clean, jsx-a11y clean)",
  "build": "pass (vite build + PWA injectManifest)",
  "size": "pass (app js 196.64/260 kB, analytics 94.79/115 kB, admin 63.97/70 kB brotli — WI-4 adds only pure/hook/client with no leaflet import, eager bundle unaffected)",
  "notes": "TDD RED->GREEN per cycle, each RED observed on the single file: centerPin (pure) -> getGeoReverse (client) -> useReverseGeocode (hook) -> useSuggest drop-hasToken (inverted test RED) -> i18n+parity. getGeoReverse mirrors getGeoSuggest (URLSearchParams + apiRequest); coords rounded to 4 decimals via Math.round(x*1e4)/1e4 in BOTH the client fn and the hook query key so the wire value equals the key (backend Math.Round(..,4)). No JS locale trap for the query string: String(number)/URLSearchParams is always dot-decimal (unlike C#), asserted by the -33.8/151.2 test. centerPin.ts is strictly pure: resolveCenterSource(permission,gps,config) decision + createMoveendDebouncer(delay,emit) setTimeout debouncer factory; it NEVER calls navigator.geolocation/permissions or Leaflet (those land in the WI-2 lazy map / UC-015 consumers per the WI notes). No component added -> no axe test (task's hooks/pure-only clause). getGeoReverse test spies on global fetch rather than vi.mock apiRequest, because an intra-module apiRequest call binds to the real function not the mocked export (documented gotcha). useSuggest gate dropped: removed the authStorage import + hasToken; enabled is now just >=3 chars + 300ms debounce. UseSuggestResult API unchanged and AddressAutocomplete does not itself gate on a token (grep-confirmed), so no consumer edit. WI-4 owns only customer.shell.{locating,gpsDenied,gpsUnavailable}, added to all six locales as real translations (de-DE umlauts are fine, not Czech-leftover markers); parity green. RUNTIME dependency on WI-1 (anonymous geo succeeds only once auth relaxed) is documented, NOT a build/test dep — tests mock the client. Overwrote a stale UC-012 WI-4 handoff at this path (WI numbers collide across UCs per CLAUDE.md; recover UC-012 detail from git history if needed). Staged only WI-4 files + this handoff; the six i18n JSONs re-staged over WI-2/3."
}
