{
  "wi_id": "WI-7",
  "files_changed": [
    "web/src/shared/i18n/LanguageSelector.tsx",
    "web/src/shared/i18n/LanguageSelector.test.tsx",
    "web/src/app/AppLayout.tsx",
    "web/src/app/AppLayout.test.tsx",
    "web/src/features/driver/settings/DriverSettingsPage.tsx",
    "web/src/features/driver/settings/DriverSettingsPage.test.tsx",
    "web/src/features/customer/shell/CustomerLayout.tsx",
    "web/src/features/customer/shell/CustomerLayout.test.tsx"
  ],
  "tests_added": 8,
  "tests_passing": true,
  "coverage_delta": "unmeasured (no coverage run in this WI; full suite 1499 passing)",
  "hit_max_turns": false,
  "notes": "Shared LanguageSelector (web/src/shared/i18n/LanguageSelector.tsx): styled NATIVE <select> mirroring VehicleSelector's styled-select pattern (theme tokens only). Iterates SUPPORTED_LOCALES registry (AC#8 - no hardcoded locale list), one <option> per entry showing nativeName. value={current} from useLanguage(), onChange -> setLanguage (cast e.target.value as LocaleCode - registry-constrained, the one legitimate narrowing). aria-label={t('common.language')}. min-height: theme.touchTargets.min (48px). Per advisor: DROPPED width:100% from the VehicleSelector template - the component sits in the dispatcher's horizontal <Nav> flex row and the customer's header row, where 100% width would blow out the layout; intrinsic width instead. No visible Label/Wrapper (aria-label is the single accessible name). PLACEMENTS: (1) Dispatcher - web/src/app/AppLayout.tsx <Nav>, inserted before the mute/logout NavButtons. (2) Driver - web/src/features/driver/settings/DriverSettingsPage.tsx, a new Section+SectionTitle(h2, t('common.language')) block styled identically to NavAppPreference's Section/Title, placed above NavAppPreference (settings section, not bottom nav); h2 is visual chrome, accessible name stays the select's aria-label 'Jazyk'. (3) Customer - web/src/features/customer/shell/CustomerLayout.tsx, wrapped LanguageSelector + CallButton in a new HeaderActions flex div on the right of the space-between header, so the selector sits next to CallButton without disturbing the Brand-left/actions-right layout. No cross-feature imports: component lives in shared/i18n, hosts import from '../shared/i18n/LanguageSelector' (or relative). TDD ladder: LanguageSelector.test.tsx RED (module missing) -> GREEN (5 tests: six native-name options, accessible name 'Jazyk' via combobox role, value reflects i18n.language, selecting de-DE switches i18n + persists app.language to localStorage asserted via waitFor on the async i18n.changeLanguage, axe clean via shared helper). Per advisor: asserted the OBSERVABLE (i18n.language + localStorage 'app.language' key from WI-2) rather than mocking languageStorage; skipped a computed-style 48px assertion (styled-components class rules don't resolve in jsdom getComputedStyle - would be vacuous/brittle; no such precedent test exists; token usage + axe are the guarantee). afterEach restores i18n.changeLanguage('cs-CZ') AND localStorage.clear() so the Czech baseline holds for the other 218 suites. Each of the three host tests extended with a 'renders the language selector' assertion using getByRole('combobox', { name: 'Jazyk' }) (native single-select maps to combobox role) - all RED before placement, GREEN after; host suites stay green. AXE: clean - vitest-axe (shared configured helper, color-contrast disabled per jsdom) reports no violations on LanguageSelector and on DriverSettingsPage (which now hosts it). MANUAL-VERIFY (out of gate scope, for reviewer): customer header at 320px viewport now holds Brand + LanguageSelector + CallButton(min-height 64px) under justify-content:space-between - Shell has overflow-x:hidden so it cannot scroll-break, but the three-item crowding was NOT visually eyeballed; confirm via manual Lighthouse/visual check. STAGED web/ only (git add web/); nothing in api/; NOT committed.",
  "verification_output": "vitest --filter LanguageSelector: Test Files 1 passed (1); Tests 5 passed (5); EXIT=0 (re-run fresh). Full suite (Czech baseline): Test Files 219 passed (219); Tests 1499 passed (1499). tsc --noEmit: clean. lint (eslint --max-warnings 0 + check:geo): clean."
}
