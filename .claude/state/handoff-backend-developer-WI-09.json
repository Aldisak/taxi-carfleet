{
  "wi_id": "WI-09",
  "files_changed": [
    "api/src/Taxi.Api/Features/Analytics/GetCustomers/GetCustomersEndpoint.cs",
    "api/src/Taxi.Api/Features/Analytics/GetCustomers/GetCustomersResponse.cs",
    "api/tests/Taxi.Api.Tests/Analytics/GetCustomersTests.cs",
    "api/tests/Taxi.Api.Tests/Seed/SeedAndEndToEndTests.cs",
    "docs/api.md"
  ],
  "tests_added": 8,
  "tests_passing": 538,
  "coverage_delta": null,
  "hit_max_turns": false,
  "notes": "WI-09 (UC-009) — GET /api/v1/analytics/customers (FleetAdmin): new-vs-returning rides, repeat rate, frequency distribution, monthly cohort triangle, top customers, ratings analysis, GDPR anonymized exclusion.\n\nRESUMED from a killed agent. Prior handoff claimed 6 tests / 536 passing — but GetCustomersTests.cs had 8 [Fact] methods (including HandleAsync_RatingsAnalysis_HasAvgTrendAndWorstRated and HandleAsync_NewVsReturningBuckets_SplitsPerBucket), the latter being the dead RED test. The endpoint was missing the per-bucket new-vs-returning series, causing CS7036 compile error (GetCustomersResponse 9th ctor param not passed).\n\n== GREEN STEP DELIVERED ==\n\nAdded per-bucket new-vs-returning series (NewVsReturningBuckets) to the endpoint:\n\n1. SQL query via FormattableStringFactory (truncUnit cannot be a SQL parameter) groups by Prague-local bucket + identity, excluding anonymized (customer_phone != '+420000000000'). Result type: NewVsReturningRawRow(DateTime Bucket, string Identity, int Rides) — single-word field names match snake_case EF SqlQuery mapping convention.\n\n2. In-memory classification: reuses firstRideMap (already computed for new/returning global counts) — isReturning = firstRideMap[identity].firstRide < winStart.\n\n3. Added NewVsReturningBuckets to ComputedBlock (private record shared between current + prior windows).\n\n4. Wired into both Send.OkAsync (GetCustomersResponse 9th arg) and the compare=true Prior block (GetCustomersPriorDto).\n\n5. GetCustomersPriorDto updated to include NewVsReturningBuckets field + XML <param> doc (required by -warnaserror CS1573).\n\n== FULL RESPONSE SHAPE ==\n\nVOLUME (includes anonymized orders):\n- TotalRides, TotalRevenueCzk\n\nIDENTITY-BASED METRICS (excludes anonymized phone '+420000000000'):\n- NewIdentities, ReturningIdentities, RepeatRate\n- FreqOne, FreqTwoToFive, FreqSixPlus\n- NewVsReturningBuckets: List<NewVsReturningBucketDto> — per granularity-bucket counts of rides by new vs returning identities\n\nCOHORT TRIANGLE (customer_user_id only, excludes anonymized and phone-only):\n- CohortRows: AcqMonthLabel (yyyy-MM), MonthsSince (0..5), ActiveCustomers\n\nTOP CUSTOMERS (take 20, excludes anonymized):\n- TopCustomers: CustomerUserId, CustomerPhone, CustomerName, Rides, RevenueCzk\n\nRATINGS ANALYSIS (all completed rides including anonymized):\n- Ratings: TotalRated, AvgRating, Distribution, AvgTrend, WorstRated\n\nCOMPARE:\n- Prior: GetCustomersPriorDto? — all sections recomputed for prior equal-length window when compare=true\n\n== VERIFICATION ==\n\ndotnet test --filter FullyQualifiedName~Analytics.GetCustomers: 8 passed, 0 failed, 0 skipped.\ndotnet build -warnaserror: 0 warnings, 0 errors.\nFull suite: 538 passed, 0 failed, 4 skipped (perf opt-in).",
  "verification_output": "dotnet test --filter FullyQualifiedName~Analytics.GetCustomers: 8 passed, 0 failed, 0 skipped, exit 0. dotnet build -warnaserror: 0 warnings, 0 errors, exit 0. Full suite: 538 passed, 0 failed, 4 skipped (perf opt-in)."
}
