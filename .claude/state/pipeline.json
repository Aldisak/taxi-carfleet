{
  "version": 1,
  "uc_id": "UC-018",
  "title": "Location-relevant address autocomplete (thread near hint end-to-end)",
  "stack": "api+web",
  "complexity_tag": "moderate",
  "phase": "done",
  "work_items_doc": "docs/specs/done/018_UC_018_location-relevant-suggest-work-items.md",
  "topo_order": "WI-1 lane:api (thread near through IMapyClient/GeoService/MapyClient + preferNear param + tests) -> WI-2 lane:web (getGeoSuggest near + pure suggestLocation helper + useSuggest wiring + tests, dep WI-1 for the contract). Sequential 1->2.",
  "gate_c": "round-1 blocks_merge=false. Highest-risk (coordinate-order lat,lng vs Mapy lng,lat) VERIFIED sound at every hop with value assertions (V-1). Only 1 LOW F-1: mid-signature near param position breaks all positional call sites at compile time (coverage-complete, no action) + reminder to also edit IMapyClient.cs interface. Conductor resolution: ACCEPT, no findings need folding beyond the F-1/V-4 reminder to edit the interface + all 5 MapyClientHandlerTests call sites (folded into WI-1 dispatch). No designer round-2.",
  "spec_path": "docs/specs/done/018_UC_018_location-relevant-suggest.md",
  "assignment_path": "docs/specs/done/018_UC_018_location-relevant-suggest.md",
  "branch": "uc-018-location-relevant-suggest (off main @ 0dda455)",
  "gates_passed": [
    "A (autonomous authority): pre-authored spec docs/specs/018 (thread `near` hint frontend->SuggestEndpoint->GeoService->MapyClient->Mapy v1/suggest location-preference param; frontend supplies map-center->GPS->fleet-config-center via a pure helper; InvariantCulture coord formatting; cache key already includes near) + directive 2026-09-16 'continue autonomously on all UCs in /todo, numeric order' — approved as-is",
    "B (autonomous): designer 2 WIs by lane (WI-1 api -> WI-2 web), acyclic; flagged coordinate-order asymmetry (frontend lat,lng vs Mapy lng,lat)",
    "C (autonomous): design-review round-1 blocks_merge=false (coordinate-order verified sound, V-1); 1 LOW (signature position, no action) + reminder to edit IMapyClient.cs interface folded into WI-1; no designer round-2",
    "D (autonomous): both WIs implemented + impl-reviewed blocks_merge=false ZERO findings each. WI-1 (api): interface+MapyClient preferNear lng-first+GeoService both-paths+4 tests. WI-2 (web): getGeoSuggest lat-first+pure suggestLocation 2-dec rounding+useSuggest query key+wiring; shared/ has no feature import (inline structural type). Coordinate-order asymmetry verified correct at both ends.",
    "E (autonomous): Phase-5 BOTH lane gates PASS. Backend: build -warnaserror 0 warn, dotnet test 740 passed/4 skipped/0 fail. Web: lint 0, tsc 0, vitest 231 files/1664 tests/0 fail, build ok, size app 195.1/260+analytics 94.81/115+admin 63.89/70 kB. No e2e (ranking change, not flow/DOM — AC#5)."
  ],
  "autonomous_mode": true,
  "autonomous_mode_authority": "user directive 2026-09-16 (autonomous on all queued UCs, numeric order, parallel only if independent): gates A-E pre-approved; ship = commit + ff-merge to main (no remote). Sequential.",
  "multi_uc_queue": [
    "UC-014 customer-map-shell - DONE (main @ 60513f8)",
    "UC-015 customer-order-flow (web) - DONE (main @ a1af1e2)",
    "UC-016 customer-live-tracking (web) - DONE (main @ 76ac2bf)",
    "UC-017 auto-dispatch (api) - DONE (main @ 0dda455)",
    "UC-018 location-relevant-suggest (api+web) - DONE (ship: commit + ff-merge to main)",
    "UC-019 driver-map-first (web, dep UC-014) - queued"
  ],
  "complexity_rationale": "Mixed-lane (api+web) but contained plumbing: the `near` hint is ALMOST wired already (SuggestEndpoint parses near + passes to GeoService; cache key already includes near) — the fix connects the disconnected middle. Backend: extend IMapyClient.SuggestAsync signature (+ update FakeMapyClient/CountingMapyClient/all call sites), GeoService forwards near on BOTH paths (Guid.Empty fleetless bypass + cached-factory), MapyClient appends Mapy location-preference param (best-effort exact param name — verify vs live Mapy docs; InvariantCulture coord format). Frontend: getGeoSuggest(q, near?) appends &near, a pure location-selection helper (map-center->GPS->config-center), useSuggest supplies it + query key includes rounded near. No new architecture, no new entity, no result-shape change. Moderate; no kit specialists.",
  "key_constraints": "No change to suggest route/verb/policy/result shape, degradation, or rate limiter. IMapyClient.SuggestAsync signature change -> update ALL implementers (MapyClient, FakeMapyClient, CountingMapyClient) + all call sites (verb-change-breaks-hardcoded-callers lesson: grep before). GeoService forwards near on BOTH the Guid.Empty fleetless bypass AND the cached-factory path (UC-010 WI-07 note). MapyClient location-preference param is BEST-EFFORT — verify exact param name/format against live Mapy.com REST API docs before finalizing (MapyConstants/UC-010 WI-09 precedent); InvariantCulture for coord formatting (the FastEndpoints/JSON locale traps). Frontend: pure tested location-selection helper (map-center -> GPS -> useGeoConfig fleet-center); useSuggest query key includes ROUNDED near (avoid cache thrash on tiny map nudges); debounce unchanged. Cache key already includes near (keep per-location caching). i18n: none expected (no new user-facing string). Quality gate: BOTH lanes — dotnet build -warnaserror + dotnet test (Testcontainers, Docker up); web tsc + lint(0 warn) + vitest + build + size. AC#5 does NOT require e2e (no user-flow DOM change) -> host-postgres:5432 conflict irrelevant for this UC. git add: use EXPLICIT file paths, never `git add <dir>` (advisor UC-017 lesson — avoids sweeping launchSettings/README/todo).",
  "in_progress_wis": [],
  "completed_wis": [
    "WI-1 (api): IMapyClient.SuggestAsync interface extended with (double Lat,double Lng)? near; MapyClient appends preferNear={lng},{lat}&preferNearPrecision=5000 (lng-first flip once, InvariantCulture dot-decimal); GeoService forwards near on BOTH Guid.Empty bypass + cache-factory paths; FakeMapyClient.LastSuggestNear capture; all 5 MapyClientHandlerTests call sites updated; 4 new tests (2 URL present/absent+coord-order w/ NotContain lat-first guard, 2 forwarding). RED observed; build -warnaserror 0 warn + 139 Geo tests green; impl-review blocks_merge=false ZERO findings."
  ],
  "predecessors": "UC-001..017 all on main @ 0dda455",
  "pre_existing_worktree_note": "Never revert/commit: README.md (untracked), api/src/Taxi.Api/Properties/launchSettings.json (modified). docs/specs/todo/019 = remaining queued spec (untracked, survive branch switches). Mixed-lane: WIs carry lane:api (backend-developer) or lane:web (frontend-developer). Host postgresql-x64-17 STOPPED (irrelevant to this UC — no e2e, and dotnet test uses Testcontainers ephemeral ports).",
  "updated_at": "2026-09-17"
}
