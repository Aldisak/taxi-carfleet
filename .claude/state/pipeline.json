{
  "version": 1,
  "uc_id": "UC-013",
  "title": "Development-only customer OTP code log",
  "stack": "api",
  "complexity_tag": "routine",
  "phase": "done",
  "spec_path": "docs/specs/done/013_UC_013_dev-otp-log.md",
  "assignment_path": "docs/specs/done/013_UC_013_dev-otp-log.md",
  "spec_final": "docs/specs/done/013_UC_013_dev-otp-log.md",
  "work_items_doc": "docs/specs/done/013_UC_013_dev-otp-log-work-items.md",
  "gates_final": [
    "A+B combined (routine deferred-A): user approved spec + single WI",
    "C: 2 rounds at user request (round-1 1 MEDIUM+2 LOW -> designer amend -> round-2 PASS 0 findings); user kept design review (declined fast-path)",
    "D: WI-1 RED-observed TDD, impl-review blocks_merge=false (1 LOW cosmetic)",
    "E: backend build -warnaserror clean + 692 pass/0 fail/4 skip (Phase 5 fixed a pre-existing UC-005 order-dependent flake test-side; production code untouched). No web gate (backend-only)."
  ],
  "ship_note": "No git remote -> local commit on uc-013-dev-otp-log + ff-merge to main (UC-009..012 precedent). Commit EXCLUDES pre-existing non-UC-013 files: README.md (untracked) and api/src/Taxi.Api/Properties/launchSettings.json (modified) - both the user's, left uncommitted. Includes the NotificationDispatchJobTests.cs flake fix (test robustness, surfaced by this gate).",
  "topo_order": "WI-1 only (api, S)",
  "branch": "uc-013-dev-otp-log (off main @ 3555a7a)",
  "gates_passed": [
    "A+B combined (2026-09-15, routine deferred-A path): user approved spec + single WI via one AskUserQuestion; spec moved todo/ -> in-progress/",
    "fast-path declined: user kept design review (default)",
    "C (2026-09-15, 2 rounds at user request): round-1 blocks_merge=false with 1 MEDIUM (DI-selection untested) + 2 LOW (dev-branch phone PII, stale XML summary); user chose formal designer amend + re-review; round-2 PASS zero findings (amendments verified incl. IHostEnvironment-stub DI trap). User approved implementation."
  ],
  "gate_a_deferred": "complexity_tag=routine (user-chosen 2026-09-15) -> Gate A deferred per SKILL.md; spec+work-items presented together after designer returns. Spec was authored interactively with the user this session (root cause traced: OTP masked in logs by design + hashed in DB; user chose the Development-only code-log option).",
  "key_constraints": "Backend-only. OTP may appear in logs ONLY when IHostEnvironment.IsDevelopment() (hard guard in the sender) AND Sms:DevLogCode=true (DI flag gate in AuthFeatureConfiguration). RequestCodeEndpoint unchanged. The Notifications-namespace SMS senders (GoSms/SmsBrana/operational ConsoleSmsSender) are OUT of scope — only the Auth-path Infrastructure/Sms sender family. Non-dev environments stay masked even with the flag on (unit-tested). User-local dotnet SDK required (bare dotnet = SDK 9 NETSDK1045).",
  "in_progress_wis": [],
  "completed_wis": [
    "WI-1 DevConsoleSmsSender (dev->full body log, non-dev->masked marker identical to ConsoleSmsSender, phone masked last-4 in BOTH branches, IHostEnvironment.IsDevelopment() production guard) + Sms:DevLogCode flag gate in AuthFeatureConfiguration (XML summary rewritten per AC#6) + appsettings.Development.json flag + docs/API-KEYS.md §2 note; 5 tests (2 behavior incl. Production-masks pin + 3 DI-selection with IHostEnvironment stub) via hand-rolled ILogger spy; RED observed (CS0246); build -warnaserror clean; impl-review round-1 blocks_merge=false (1 LOW: LoggerSpy field naming). During Phase 5 surfaced a PRE-EXISTING order-dependent flake (NotificationDispatchJobTests.NotificationDispatchJob_DuplicateOutbox_InvokesSenderOnce, from UC-005) where the shared-DB job tick dispatched leaked outbox rows through the test's recording sender -> fixed test-side (unique per-run phone + pre-seed cleanup of leaked Queued rows + phone-scoped Sent assertion); production code untouched."
  ],
  "phase5_gate": "backend build -warnaserror clean (Release; Debug apphost was locked by a running Taxi.Api dev server -> built Release); dotnet test 692 passed / 0 failed / 4 skipped. No web block (backend-only UC, 0 web files). dotnet format: informational, not run to completion (non-blocking per Phase 5).",
  "predecessors": "UC-001..012 all on main @ 3555a7a",
  "pre_existing_worktree_note": "Uncommitted at pipeline start (NOT part of UC-013, do not revert/commit): README.md (untracked, user's), api/src/Taxi.Api/Properties/launchSettings.json (modified, user's)",
  "updated_at": "2026-09-15"
}
