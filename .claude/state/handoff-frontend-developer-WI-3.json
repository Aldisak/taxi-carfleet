{
  "wi": "WI-3",
  "uc": "UC-016",
  "status": "complete",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/features/customer/shell/CustomerMapBackground.tsx",
    "web/src/features/customer/shell/CustomerMapBackground.test.tsx",
    "web/src/features/customer/shell/CustomerMapShell.tsx",
    "web/src/features/customer/shell/CustomerMapShell.test.tsx",
    ".claude/state/handoff-frontend-developer-WI-3.json"
  ],
  "tests_added": 7,
  "red_observed": true,
  "tsc": "pass",
  "lint": "pass (0 warnings; check:geo OK)",
  "verification": "vitest --filter features/customer/shell -> exit 0 (12 files, 71 tests, fresh re-run)",
  "notes": "Added optional DATA props carMarker: LatLng | null and pickupMarker: LatLng | null to both CustomerMapShell and CustomerMapBackground (LatLng from ./mapCamera, matching cameraTarget). Markers + divIcons are constructed INSIDE CustomerMapBackground (the sole leaflet importer under the shell); the page never imports react-leaflet/leaflet. PICKUP_ICON/CAR_ICON migrated verbatim from tracking/TrackingMapInner as module-level L.divIcon consts (created once, no useMemo per rules/web-performance.md#memoization-policy) with classNames 'tracking-pickup-icon' + 'tracking-car-icon' kept EXACT ('.tracking-car-icon' is the load-bearing e2e locator in customer.spec.ts). Marker titles use i18n keys customer.tracking.pickupLabel/carLabel (already existed; no locale files touched, parity trivially green). SMOOTH INTERPOLATION (AC#2): AnimatedCarMarker is a small component inside the lazy chunk holding the interpolated position in LOCAL useState; it consumes the raw carMarker target and animates frame-by-frame via markerInterpolation.interpolateCoord + requestAnimationFrame. Per advisor #2 the rAF time source is the timestamp rAF passes the callback (startTs captured on first frame, elapsed = ts - startTs) so the anim is self-consistent and the test drives it with a manual rAF mock flushing explicit timestamps (vitest does NOT fake rAF/performance.now). Interpolated position is NEVER threaded back up through the shell (no per-frame shell re-render, rules/web-performance.md#high-frequency-events). First fix snaps (no prev). prev read via ref-to-latest-rendered so effect deps are [target] only (exhaustive-deps disable, mirrors MoveendController); cancelAnimationFrame on new target + unmount. REDUCED MOTION (AC#3): prefersReducedMotion() reads window.matchMedia('(prefers-reduced-motion: reduce)') (guarded for jsdom-without-matchMedia); shouldAnimate(false-when-reduced) gates the rAF loop -> marker jumps directly to each new target, no frames scheduled (test asserts rafCallbacks length 0). CAMERA (AC#4): unchanged - markers and camera are INDEPENDENT; car/pickup drive only <Marker> rendering, camera stays driven solely by the existing cameraTarget prop + CameraController. Did NOT derive cameraTarget from car/pickup here (the page composes [car, pickup] in WI-4) - avoids double-driving the camera and keeps the existing setView/fitBounds tests green. Test details: extended the react-leaflet mock with a Marker stub echoing position (JSON), icon className (data-icon-class), and title as a PLAIN title attr (getByTitle, NOT aria-label -> avoids the aria-prohibited-attr axe trap, CLAUDE.md UC-014 WI-2). MapContainer mock keeps role='application'. matchMedia mocked per-test (no global polyfill in test-setup; SearchingLoader is CSS-only so no reuse precedent). Interpolation test flushes >=2 frames: frame at ts=0 establishes start (elapsed 0 -> prev), frame at MARKER_ANIMATION_MS/2 asserts strictly-between on lat AND lng, frame at MARKER_ANIMATION_MS asserts === target (interpolateCoord clamps). MARKER_ANIMATION_MS (3000) exported for the test to pick timestamps. axe test renders under reduced-motion (snap, no pending frame -> no act warning). Shell test's CustomerMapBackground mock extended to echo data-car-marker/data-pickup-marker + a threading assertion (mirrors data-camera-target). Leaflet-lazy discipline intact: CustomerMapBackground is imported ONLY via lazy(() => import('./CustomerMapBackground')) in CustomerMapShell (grep-verified); the new markerInterpolation import is eager-safe (imports nothing from leaflet). Existing cameraTarget/moveend/center-pin/ensureFleetSlug-once tests all stay green. Scope: ONLY the 4 shell files touched; tracking/ files, the page, router, locale JSONs, and e2e untouched. Pre-existing act() warnings in the shell test output come from Suspense/react-router resolving (not from the new tests, which wrap flushFrame in act()). NOTE: overwrote a stale UC-015-era handoff-frontend-developer-WI-3.json (WI numbers repeat across UCs — documented collision in CLAUDE.md); UC-015 WI-3 detail recoverable from git history."
}
