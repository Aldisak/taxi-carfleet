{
  "wi": "WI-3",
  "wi_id": "WI-3",
  "uc": "UC-019",
  "status": "complete",
  "lane": "web",
  "files_changed": [
    "web/src/features/driver/map/DriverMapInner.tsx",
    "web/src/features/driver/map/DriverMapInner.test.tsx",
    "web/src/features/driver/map/useRouteGeometry.ts",
    "web/src/features/driver/map/useRouteGeometry.test.ts"
  ],
  "tests_added": [
    "web/src/features/driver/map/useRouteGeometry.test.ts (5 tests: fetch toPickup->geometry+duration, skip-when-leg-null, skip-when-own-null, degrade-on-throw, refetch toPickup->toDropoff with new endpoint)",
    "web/src/features/driver/map/DriverMapInner.test.tsx (10 tests: default aria-label, Polyline present >=2 / absent null / absent 1-point, 3-marker/1-marker/no-dropoff-marker cases, fitBounds 2-pt / setView 1-pt camera, axe)"
  ],
  "tests_passing": true,
  "red_observed": true,
  "red_details": [
    "useRouteGeometry: RED observed - 'Failed to resolve import ./useRouteGeometry' (0 tests collected) before impl; GREEN 5/5 after.",
    "DriverMapInner: RED observed - import of ./DriverMapInner failed transform (0 tests collected) before impl; GREEN 10/10 after."
  ],
  "coverage_delta": "2 new modules fully covered: useRouteGeometry (fetch/skip/degrade/refetch paths) + DriverMapInner (Polyline threshold, marker matrix, camera intents, axe). No existing coverage changed.",
  "tsc": "pass for all WI-3 files (0 errors). Whole-project tsc reports exactly ONE error and it is NOT mine: src/features/driver/offer/OfferCard.test.tsx(1,48) TS6133 unused 'afterEach' import — an untracked WI-2 file (git '??'), outside WI-3 files_touched; must not edit per scope. `tsc | grep -v OfferCard.test.tsx | grep -c 'error TS'` == 0 confirms zero WI-3 errors.",
  "lint": "pass (0 warnings) — scoped eslint over the 4 WI-3 files with --max-warnings 0 produced no output.",
  "verification": { "tool": "vitest", "filter": "features/driver/map", "exit_code": 0, "result": "3 files / 19 tests passed" },
  "verification_output": "npm run --prefix web test -- \"features/driver/map\" => 3 files (driverMapView.test.ts 4, useRouteGeometry.test.ts 5, DriverMapInner.test.tsx 10) / 19 tests passed, exit 0 (re-run fresh). Scoped eslint over the 4 WI-3 files => 0 warnings. tsc: 0 WI-3 errors (only the pre-existing untracked WI-2 OfferCard.test.tsx TS6133 remains, out of scope).",
  "hit_max_turns": false,
  "new_i18n_keys": [],
  "notes": "TWO deliverables via RED->GREEN->REFACTOR (RED = import-resolution failure, observed per module before each impl). (1) DriverMapInner.tsx — presentational DEFAULT-EXPORT (props only, never a fetch): props {own,pickup,dropoff,routeGeometry,cameraTarget,ariaLabel?}. Renders <MapyMap ariaLabel={ariaLabel ?? t('driver.map.label')}> with, as CHILDREN: a react-leaflet <Polyline positions={routeGeometry as [number,number][]}> drawn ONLY when routeGeometry != null && length >= 2 (the codebase's FIRST Polyline); own/pickup/dropoff <Marker>s (each null-guarded); and a headless CameraController. DESIGN-REVIEW F-1 HONORED: OWN_ICON/PICKUP_ICON/DROPOFF_ICON L.divIcon consts are COPIED VERBATIM (module-level, no useMemo) from RideMapInner INTO DriverMapInner.tsx — NOT imported — so RideMapInner has zero new importers and WI-5 can delete it without a dangling tsc break. classNames preserved (ride-own-icon / ride-pickup-icon / ride-dropoff-icon). Marker labels use t('driver.map.ownLabel'|'pickupLabel'|'dropoffLabel') as the `title` prop (NOT aria-label — aria-label on a Marker re-triggers the aria-prohibited-attr axe trap; the region's aria-label carries a11y). CameraController is COPIED (not shared) from CustomerMapBackground — react-leaflet-coupled (useMap + prev-key ref), must live in the lazy chunk; only the pure cameraIntent it calls comes from '../../../shared/map/mapCamera' (the WI-1 promoted module, never the customer ./mapCamera shim, never cross-feature). DriverMapInner is purely presentational: it does NOT call selectDriverMapCamera/legEndpoint — cameraTarget is fed in ready; WI-5 wires selectRouteLeg+legEndpoint+selectDriverMapCamera. (2) useRouteGeometry.ts — EAGER hook, NO leaflet import: given {leg,own,order} it resolves end=legEndpoint(leg,order) (WI-1) and calls getGeoRoute(own->end), returning {geometry:number[][]|null, durationSeconds:number|null, isLoading}. Skips the fetch (geometry null, isLoading false, getGeoRoute NOT called) when leg===null || own==null || end==null. DEGRADES SILENTLY on getGeoRoute throw (502 Geo.RouteUnavailable) -> geometry null, no throw to caller (map still shows markers) — mirrors useOfferRoute's cancelled-flag + try/catch. CRITICAL (advisor): the effect keys on the PRIMITIVE coords [leg, shouldFetch, ownLat, ownLng, endLat, endLng], NOT object identity [own, order] — changing the leg toPickup->toDropoff changes end coords -> effect reruns -> refetch (test #5); depending on objects would let WI-5's fresh `own` each render storm the API. LEAFLET-LAZY CONFIRMED: DriverMapInner is the ONLY module under features/driver/ importing MapyMap/react-leaflet/leaflet; useRouteGeometry imports NEITHER; DriverMapInner is not statically imported anywhere eager (grep-clean) — it will be React.lazy-loaded by WI-5, whose npm-build is the dist gate (WI-3 cannot verify the chunk until WI-5 wires the import, per AC). TEST HARNESS: cloned CustomerMapBackground.test.tsx pattern — mocks react-leaflet (MapContainer stub carries role='application' + forwards aria-label per CLAUDE.md UC-014 WI-2; Marker/TileLayer/useMap stubs; NEW Polyline stub exposing data-positions), mocks '../../../shared/map/leafletSetup'->{} and '../../../shared/map/useGeoConfig' (returns data:CONFIG so MapyMap mounts MapContainer instead of its loading state), and renders the REAL MapyMap — NEVER mounts a real MapContainer in jsdom (CLAUDE.md UC-010 WI-15). Scope respected: only DriverMapInner.tsx (+test) and useRouteGeometry.ts (+test); did NOT edit RideMapInner (WI-5 removes it), OfferCard/RideSheet/DriverMapScreen, router, DriverLayout, or locale JSONs (keys came from WI-1). CROSS-WI NOTE: OfferCard.test.tsx (untracked WI-2 file) has a pre-existing TS6133 unused-import; it is outside WI-3 scope and was left untouched — flag for the WI-2 owner/impl-reviewer. This handoff filename previously held a stale UC-016 WI-3 result (documented filename-collision trap — WI numbers repeat across UCs); overwritten with UC-019 WI-3."
}
