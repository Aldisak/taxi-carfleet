{
  "wi": "WI-3",
  "uc": "UC-015",
  "status": "complete",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/features/customer/order/PriceSheet.tsx",
    "web/src/features/customer/order/PriceSheet.test.tsx"
  ],
  "tests_added": 14,
  "red_observed": true,
  "full_suite": "236 files, 1633 tests passed (includes i18n parity + formatStaysCs)",
  "tsc": "pass (0 errors)",
  "lint": "pass (0 warnings; check:geo OK)",
  "build": "pass (vite build + PWA injectManifest)",
  "size": "pass — app js 197.32/260 kB, analytics 94.79/115 kB, admin 63.97/70 kB (brotli)",
  "notes": "PriceSheet is a CONTROLLED surface per the designer props contract: quote is a PROP (UsePriceQuoteResult from the page's usePriceQuote({allowAnonymous:true}) in WI-4), NOT an internal usePriceQuote call — avoids WI-4 double-quoting. It DOES call useCreateOrder() + useOnlineStatus() internally; useOnlineStatus is the correct customer Order gate (no SignalR hub while ordering; web-realtime.md#offline-ux — never queue). Renders inside the UC-014 BottomSheet (open, expanded drives the collapsed/expanded options area, ariaLabelKey customer.mapOrder.sheetLabel, Escape via onClose which collapses options if open else onCancel). F4 enforced: isOrderable() type-guard narrows view to fixed|estimate BEFORE buildOrderRequest is invoked; meter/unknown/null-view/errorKey all disable Order and show the customer.mapOrder.meterUnavailable fallback. PriceRangeBadge reused unchanged: Fixed = exact price, Estimate = RANGE (low-high, never single), error via errorKey. Order-click continuation mirrors CustomOrderPage.handleSubmitClick->submit verbatim: offline -> visible role=alert offlineBlocked reason rendered persistently whenever offline (so the reason is visible without a click) + Order disabled; logged-out -> inline CustomerLoginStep in-place with all order state preserved (submit useCallback deps include passengers/whenMode/scheduledAtLocal/note/quote.view/pickup/destination so the post-login create uses current state) + auto-resume on onAuthenticated; logged-in -> buildOrderRequest + createOrder.mutateAsync -> onOrdered(publicCode) with NO navigation baked in (page owns it; WI-4 navigates to /customer/t/:code). Scheduled tooSoon/tooLate/invalid reuse validateScheduledAt; create failure -> customer.order.errorCreateFailed and stays on the sheet. Options area reuses PassengerStepper + WhenPicker (min/max via whenRules) + note input. ZERO i18n changes: all keys already landed in WI-1 (customer.mapOrder.* priceSheetTitle/order/cancel/optionsToggle/meterUnavailable/sheetLabel/loginPrompt + customer.order.* offlineBlocked/errorCreateFailed/submitting/passengersLabel/noteLabel/notePlaceholder/error*). Primary Order button min-height theme.touchTargets.primary (64px >= 48). Axe clean. Scope respected exactly: only the 2 files_touched; locale JSONs untouched (WI-1's, already staged). RED observed: import-resolution failure of ./PriceSheet before implementation, confirmed in output. Overwrote a stale UC-014 same-named WI-3 handoff (WI numbers repeat across UCs — CLAUDE.md filename-collision note)."
}
