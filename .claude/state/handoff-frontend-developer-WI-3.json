{
  "wi": "WI-3",
  "status": "done",
  "hit_max_turns": false,
  "files_changed": [
    "web/src/shared/api/client.ts",
    "web/src/features/admin/adminTenantSettingsForm.ts",
    "web/src/features/admin/adminTenantSettingsForm.test.ts",
    ".claude/state/handoff-frontend-developer-WI-3.json"
  ],
  "tests_added": [
    "web/src/features/admin/adminTenantSettingsForm.test.ts (30 tests)"
  ],
  "red_observed": "true — first scoped run (npx vitest run adminTenantSettingsForm) failed to load the module (Failed to load url ./adminTenantSettingsForm; 0 tests collected) because adminTenantSettingsForm.ts and the client DTOs did not exist yet",
  "full_suite": { "test_files": 221, "tests": 1533, "passed": 1533, "failed": 0 },
  "tsc": "clean",
  "lint": "0 warnings",
  "notes": "UC-012 WI-3 (web lane, pure-logic only — no component). client.ts gained AdminTenantSettingsDto (GET shape; mapyBrowserKey string|null, mapyServerKeyConfigured boolean, NO server-key value field), UpdateAdminTenantSettingsRequest (PUT; mapyServerKey/mapyBrowserKey string|null), getAdminTenantSettings(fleetId), putAdminTenantSettings(fleetId, req) — mirrors getAdminFleets/postCreateFleet exactly (apiRequest). New pure module adminTenantSettingsForm.ts: AdminTenantSettingsFormValues (number fields as strings, both keys as strings, mapyServerKeyConfigured carried for WI-4's hint), validateAdminTenantSettingsForm -> admin.tenant.validation.* i18n-key map, toUpdateRequest, fromDto. SECURITY (RED tests written FIRST, asserted toBeNull + not.toBe('')): toUpdateRequest maps blank AND whitespace-only input for BOTH mapyServerKey and mapyBrowserKey -> null (KEEP the stored key), never '' (which would CLEAR server-side). fromDto ALWAYS seeds mapyServerKey blank (DTO never carries it), passes mapyServerKeyConfigured through, seeds mapyBrowserKey from dto.mapyBrowserKey ?? ''. KEY TRAP AVOIDED (advisor catch): mapCenterLat/Lng validated with Number.isFinite (floats, defaults 50.08/14.42) NOT Number.isInteger — mirroring fleetSettingsForm's isInteger check per-field would have rejected the entity defaults. Integer+range fields (offerTimeout 10..600, autoDispatchAfter >=0, maxOfferRadius 1..100, smsCap >=0, smsUnitCost >=0, mapZoom 1..20, geoBudget >=0) use Number.isInteger + bounds; currency validated as exactly 3 ASCII letters (^[A-Za-z]{3}$). Design-review F4 folded in (keep-on-blank deliberate for both keys; WI-4 page documents via field hint). No locale JSON entries added — WI-4 owns those per instructions. No any; explicit return types on all exports; no fetch/axios outside client.ts; tests colocated. Handoff filename keyed on WI-<n> collided with a stale UC-011 ru-RU handoff (documented CLAUDE.md fact) — overwritten with this UC-012 WI-3 record; recover UC-011 detail from git history if needed. Did NOT touch pre-existing uncommitted tree (README.md, launchSettings.json, docs/*, api/*). Nothing committed."
}
